---
phase: 05-elasticsearch-integration
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/Search/ProductSearchService.php
  - app/Http/Controllers/Api/V1/ProductSearchController.php
  - app/Http/Requests/SearchProductsRequest.php
  - app/Models/Tenant.php
  - routes/api.php
  - tests/Feature/Search/ProductSearchTest.php
  - tests/Integration/Search/FuzzySearchTest.php
  - tests/Integration/Search/TenantScopingTest.php
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
  - SEARCH-06
  - SEARCH-07
user_setup: []

must_haves:
  truths:
    - "Agency admin can search products within a single client's catalog"
    - "Search returns results in sub-second time (< 500ms for typical queries)"
    - "Search supports fuzzy matching (tolerates typos, partial matches)"
    - "Search results are paginated (20 products per page)"
    - "Search results only include products from selected client store (tenant isolation)"
    - "Search API endpoint is authenticated and rate-limited"
  artifacts:
    - path: "app/Search/ProductSearchService.php"
      provides: "Search query builder with pagination and fuzzy matching"
      exports: ["search", "createTenantIndex", "deleteTenantIndex"]
      min_lines: 80
    - path: "app/Http/Controllers/Api/V1/ProductSearchController.php"
      provides: "Search API endpoint with tenant scoping"
      exports: ["search", "reindex"]
      min_lines: 70
    - path: "app/Http/Requests/SearchProductsRequest.php"
      provides: "Search query validation"
      contains: "rules()"
      min_lines: 30
    - path: "routes/api.php"
      provides: "Search API routes"
      contains: "Route::get('/search', [ProductSearchController::class, 'search'])"
  key_links:
    - from: "app/Http/Controllers/Api/V1/ProductSearchController.php"
      to: "app/Search/ProductSearchService.php"
      via: "constructor injection"
      pattern: "ProductSearchService"
    - from: "app/Search/ProductSearchService.php"
      to: "app/Models/Product.php"
      via: "Product::search() Scout method"
      pattern: "Product::search\("
    - from: "app/Search/ProductSearchService.php"
      to: "app/Search/IndexManager.php"
      via: "IndexManager dependency"
      pattern: "IndexManager"
    - from: "tests/Feature/Search/ProductSearchTest.php"
      to: "app/Http/Controllers/Api/V1/ProductSearchController.php"
      via: "feature test coverage"
      pattern: "ProductSearchTest"
---

<objective>
Build product search API with fuzzy matching, pagination, and strict tenant isolation delivering sub-second performance.

Purpose: Enable agency admins to search product catalogs within a specific client store with typo-tolerant fuzzy matching, fast sub-second response times, and guaranteed tenant data isolation.

Output: ProductSearchService for search logic, ProductSearchController with authenticated API endpoint, SearchProductsRequest validation, and comprehensive tests for fuzzy matching, pagination, and tenant scoping.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-elasticsearch-integration/05-RESEARCH.md
@.planning/phases/05-elasticsearch-integration/05-01-PLAN.md
@.planning/phases/04-background-processing-infrastructure/04-03-SUMMARY.md
@.planning/phases/03-tenant-management/03-03-SUMMARY.md
@.planning/phases/02-authentication-api-foundation/02-02-SUMMARY.md

@app/Models/Tenant.php
@app/Engines/ElasticsearchEngine.php
@app/Search/IndexManager.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProductSearchService with fuzzy matching and pagination</name>
  <files>app/Search/ProductSearchService.php</files>
  <behavior>
    - Test 1: search() returns paginated results with data and meta
    - Test 2: search() applies pagination offset from page parameter
    - Test 3: search() limits results to per_page parameter (max 100)
    - Test 4: search() includes total, current_page, last_page in meta
    - Test 5: search() sets tenant context before searching
    - Test 6: createTenantIndex() delegates to IndexManager
    - Test 7: deleteTenantIndex() removes index via IndexManager
  </behavior>
  <action>
    Create app/Search/ProductSearchService.php:
    1. Constructor accepts IndexManager $indexManager
    2. search(Tenant $tenant, string $query, int $page = 1, int $perPage = 20): array
       - Set tenant context: Tenant::setCurrentTenant($tenant)
       - Build Scout search: Product::search($query)
       - Apply pagination: $from = ($page - 1) * $perPage, $search->take($perPage)->offset($from)
       - Execute search and get raw results for timing: $results = $search->raw()
       - Return array with 'data' => $search->get(), 'meta' => ['total' => $search->getTotal(), 'per_page' => $perPage, 'current_page' => $page, 'last_page' => ceil($search->getTotal() / $perPage), 'query' => $query, 'took' => $results['took'] ?? 0]
    3. createTenantIndex(Tenant $tenant): void - delegate to $this->indexManager->createIndex($tenant, 'products')
    4. deleteTenantIndex(Tenant $tenant): void - get index name and delete via client
    5. reindexTenantProducts(Tenant $tenant): void - delete index, recreate, chunk products and call searchable()

    Use Scout's built-in search with fuzzy matching from ElasticsearchEngine. Track query time for performance monitoring.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=product-search-service</automated>
  </verify>
  <done>
    ProductSearchService with pagination, tenant context management, index operations, and timing metrics.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SearchProductsRequest validation</name>
  <files>app/Http/Requests/SearchProductsRequest.php</files>
  <behavior>
    - Test 1: query field is required
    - Test 2: query field must be string with min 2 chars
    - Test 3: query field max 255 chars
    - Test 4: page field optional integer min 1
    - Test 5: per_page field optional integer min 1 max 100
  </behavior>
  <action>
    Create app/Http/Requests/SearchProductsRequest extending FormRequest:
    1. rules() returns:
       - 'query' => 'required|string|min:2|max:255'
       - 'page' => 'integer|min:1'
       - 'per_page' => 'integer|min:1|max:100'
    2. Override failedValidation() to return Phase 2 error format: {errors: [{field, message}]}
    3. Set default values in prepareForValidation(): page = 1, per_page = 20

    Follow Phase 2 validation pattern with field-level errors in consistent format.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=search-validation</automated>
  </verify>
  <done>
    SearchProductsRequest with validated query parameters and Phase 2 error format.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ProductSearchController with authentication and rate limiting</name>
  <files>app/Http/Controllers/Api/V1/ProductSearchController.php, routes/api.php</files>
  <behavior>
    - Test 1: GET /api/v1/tenants/{tenantId}/search returns search results
    - Test 2: Search endpoint requires authentication (401 without token)
    - Test 3: Search endpoint validates tenant exists (404 if not found)
    - Test 4: Search endpoint applies api-write rate limiter (10/min)
    - Test 5: Search returns 200 with consistent JSON structure {data, meta}
    - Test 6: Search results include timing metadata (took field)
  </behavior>
  <action>
    Create app/Http/Controllers/Api/V1/ProductSearchController extending ApiController:
    1. Constructor accepts ProductSearchService $searchService
    2. search(Request $request, string $tenantId): JsonResponse
       - Validate tenant exists: $tenant = Tenant::findOrFail($tenantId)
       - Validate request with SearchProductsRequest
       - Execute search: $results = $this->searchService->search($tenant, $request->query, (int)$request->page, (int)$request->per_page)
       - Return $this->success($results) with 200 status
    3. Add route: Route::get('/tenants/{tenantId}/search', [ProductSearchController::class, 'search'])->middleware(['auth:sanctum', 'throttle:api-write'])

    Extend ApiController for Phase 2 response helpers. Use throttle:api-write for rate limiting.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Feature --group=search-api</automated>
  </verify>
  <done>
    ProductSearchController with authenticated search endpoint, validation, and rate limiting.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create fuzzy matching integration tests</name>
  <files>tests/Integration/Search/FuzzySearchTest.php</files>
  <behavior>
    - Test 1: Search finds products with typos (e.g., "iphne" matches "iphone")
    - Test 2: Search handles partial matches (e.g., "phone" matches "smartphone")
    - Test 3: Search respects field weighting (name^2 boosts name matches)
    - Test 4: Search returns sub-second performance (< 500ms for 1000 products)
    - Test 5: Fuzzy matching with prefix_length 2 requires exact first 2 chars
  </behavior>
  <action>
    Create tests/Integration/Search/FuzzySearchTest.php:
    1. test_fuzzy_matching_tolerates_typos() - create product "iPhone 15 Pro", search "iphne", assert result found
    2. test_partial_matching_works() - create product "Smartphone Case", search "phone", assert result found
    3. test_field_weighting_boosts_name_matches() - create products with same term in name vs description, search term, assert name result ranks higher
    4. test_search_performance_sub_second() - create 1000 products, execute search, assert $results['took'] < 500
    5. test_prefix_length_requires_exact_start() - search "xyz" for "abc product", assert no results (first 2 chars don't match)

    Use real Elasticsearch client. Seed realistic product data. Measure query time from Elasticsearch 'took' field.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=fuzzy-search</automated>
  </verify>
  <done>
    Fuzzy matching integration tests verifying typo tolerance, partial matches, field weighting, and sub-second performance.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create tenant scoping and isolation tests</name>
  <files>tests/Integration/Search/TenantScopingTest.php</files>
  <behavior>
    - Test 1: Search only returns products from selected tenant
    - Test 2: Search results exclude products from other tenants
    - Test 3: Tenant global scope applied to Product model during search
    - Test 4: Search with different tenant IDs returns different results
    - Test 5: Index per tenant verified (separate indices for each tenant)
  </behavior>
  <action>
    Create tests/Integration/Search/TenantScopingTest.php:
    1. test_search_only_returns_tenant_products() - create tenant A with product "Product A", tenant B with product "Product B", search tenant A, assert only "Product A" returned
    2. test_search_excludes_other_tenant_products() - same setup, search tenant A, assert "Product B" NOT in results
    3. test_tenant_global_scope_applied() - verify Product::search() query includes tenant scoping
    4. test_different_tenants_different_results() - search same query across tenants, assert different results
    5. test_index_per_tenant_isolation() - query Elasticsearch indices directly, verify separate indices exist

    Create multiple tenants with products. Verify complete data isolation. Use Elasticsearch client to inspect indices.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=tenant-scoping</automated>
  </verify>
  <done>
    Tenant scoping integration tests verifying complete data isolation and index-per-tenant strategy.
  </done>
</task>

</tasks>

<verification>
1. ProductSearchService searches tenant products with pagination
2. Search API endpoint authenticated and rate-limited
3. Fuzzy matching tolerates typos and partial matches
4. Search returns sub-second performance (< 500ms)
5. Tenant isolation verified (no cross-tenant data leakage)
6. Pagination works correctly (page, per_page parameters)
7. Search returns consistent JSON format with timing metadata
8. Field weighting boosts name matches over description matches
9. All tests pass with comprehensive coverage
10. API follows Phase 2 patterns (authentication, rate limiting, response format)
</verification>

<success_criteria>
1. Agency admin can search products within single client's catalog (SEARCH-01)
2. Search returns results in sub-second time (< 500ms) (SEARCH-02)
3. Search supports fuzzy matching (typos, partial matches) (SEARCH-03)
4. Search results are paginated (20 products per page) (SEARCH-04)
5. Search results only include products from selected client store (SEARCH-07)
6. Elasticsearch index scoped per tenant (SEARCH-06)
7. API authenticated and rate-limited per Phase 2 patterns
8. Integration tests verify fuzzy matching and tenant isolation
</success_criteria>

<output>
After completion, create `.planning/phases/05-elasticsearch-integration/05-02-SUMMARY.md`
</output>
