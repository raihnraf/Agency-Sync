---
phase: 05-elasticsearch-integration
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/Unit/Search/ElasticsearchEngineTest.php
  - tests/Feature/Search/SearchApiTest.php
  - tests/Integration/Search/FuzzySearchTest.php
  - tests/Integration/Search/TenantScopingTest.php
  - tests/Feature/Search/AsyncIndexingTest.php
  - tests/bootstrap.php
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All test stub files exist for Nyquist compliance"
    - "Test files are structured with proper namespaces and test methods"
    - "Elasticsearch test factory provides client for test isolation"
    - "Tests can run with real Elasticsearch client from Docker container"
  artifacts:
    - path: "tests/Unit/Search/ElasticsearchEngineTest.php"
      provides: "Unit test stub for SEARCH-05, SEARCH-07"
      contains: "class ElasticsearchEngineTest extends TestCase"
      min_lines: 30
    - path: "tests/Feature/Search/SearchApiTest.php"
      provides: "Feature test stub for SEARCH-01, SEARCH-02"
      contains: "class SearchApiTest extends TestCase"
      min_lines: 30
    - path: "tests/Integration/Search/FuzzySearchTest.php"
      provides: "Integration test stub for SEARCH-03, SEARCH-04"
      contains: "class FuzzySearchTest extends TestCase"
      min_lines: 30
    - path: "tests/Integration/Search/TenantScopingTest.php"
      provides: "Integration test stub for SEARCH-06"
      contains: "class TenantScopingTest extends TestCase"
      min_lines: 30
    - path: "tests/Feature/Search/AsyncIndexingTest.php"
      provides: "Feature test stub for QUEUE-07"
      contains: "class AsyncIndexingTest extends TestCase"
      min_lines: 30
    - path: "tests/bootstrap.php"
      provides: "Elasticsearch client factory for test isolation"
      contains: "elasticsearch"
      min_lines: 40
  key_links: []
---

<objective>
Create test infrastructure stubs for Nyquist compliance, establishing the test harness before implementing production code.

Purpose: Ensure all test files exist as documented in VALIDATION.md Wave 0 requirements, enabling TDD workflow with RED→GREEN→REFACTOR cycles. This plan satisfies the Nyquist rule that every `<verify>` element must have either an automated command or a test file created in Wave 0.

Output: Six test stub files with proper structure and namespaces, plus Elasticsearch test factory in bootstrap.php for test isolation.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-elasticsearch-integration/05-VALIDATION.md
@.planning/phases/05-elasticsearch-integration/05-RESEARCH.md

@phpunit.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stub for ElasticsearchEngine</name>
  <files>tests/Unit/Search/ElasticsearchEngineTest.php</files>
  <action>
    Create tests/Unit/Search/ElasticsearchEngineTest.php:
    1. Namespace: Tests\Unit\Search
    2. Extend: Tests\TestCase
    3. Add setUp() method with placeholder for Elasticsearch client mock
    4. Add test method stubs (all marked with $this->assertTrue(true) to pass initially):
       - test_update_indexes_models_to_tenant_index()
       - test_delete_removes_models_from_tenant_index()
       - test_search_queries_only_tenant_index()
       - test_build_query_creates_multi_match_with_fuzziness()
       - test_map_returns_collection_ordered_by_relevance()
       - test_get_total_count_returns_total_hits()
    5. Add @group annotation for group=elasticsearch-engine
    6. Add docblock explaining this file tests SEARCH-05, SEARCH-07 requirements

    DO NOT implement actual test logic - this is Wave 0 stub creation. Tests will fail initially when real implementation is added in Plan 01.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=elasticsearch-engine</automated>
  </verify>
  <done>
    ElasticsearchEngineTest.php stub exists with 6 test method placeholders, proper namespace, and group annotation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create feature test stub for Search API</name>
  <files>tests/Feature/Search/SearchApiTest.php</files>
  <action>
    Create tests/Feature/Search/SearchApiTest.php:
    1. Namespace: Tests\Feature\Search
    2. Extend: Tests\TestCase
    3. Add setUp() method with placeholder for authentication and tenant creation
    4. Add test method stubs (all marked with $this->assertTrue(true) to pass initially):
       - test_search_products_within_tenant_catalog()
       - test_search_returns_sub_second_performance()
       - test_search_requires_authentication()
       - test_search_validates_tenant_exists()
       - test_search_returns_consistent_json_structure()
    5. Add @group annotation for group=search-api
    6. Add docblock explaining this file tests SEARCH-01, SEARCH-02 requirements

    DO NOT implement actual test logic - this is Wave 0 stub creation.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Feature --group=search-api</automated>
  </verify>
  <done>
    SearchApiTest.php stub exists with 5 test method placeholders, proper namespace, and group annotation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create integration test stub for fuzzy matching</name>
  <files>tests/Integration/Search/FuzzySearchTest.php</files>
  <action>
    Create tests/Integration/Search/FuzzySearchTest.php:
    1. Namespace: Tests\Integration\Search
    2. Extend: Tests\TestCase
    3. Add setUp() method with placeholder for Elasticsearch client and product seeding
    4. Add test method stubs (all marked with $this->assertTrue(true) to pass initially):
       - test_fuzzy_matching_tolerates_typos()
       - test_partial_matching_works()
       - test_field_weighting_boosts_name_matches()
       - test_search_performance_sub_second()
       - test_prefix_length_requires_exact_start()
    5. Add @group annotation for group=fuzzy-search
    6. Add docblock explaining this file tests SEARCH-03, SEARCH-04 requirements

    DO NOT implement actual test logic - this is Wave 0 stub creation.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=fuzzy-search</automated>
  </verify>
  <done>
    FuzzySearchTest.php stub exists with 5 test method placeholders, proper namespace, and group annotation.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create integration test stub for tenant scoping</name>
  <files>tests/Integration/Search/TenantScopingTest.php</files>
  <action>
    Create tests/Integration/Search/TenantScopingTest.php:
    1. Namespace: Tests\Integration\Search
    2. Extend: Tests\TestCase
    3. Add setUp() method with placeholder for multi-tenant creation and Elasticsearch client
    4. Add test method stubs (all marked with $this->assertTrue(true) to pass initially):
       - test_search_only_returns_tenant_products()
       - test_search_excludes_other_tenant_products()
       - test_tenant_global_scope_applied()
       - test_different_tenants_different_results()
       - test_index_per_tenant_isolation()
    5. Add @group annotation for group=tenant-scoping
    6. Add docblock explaining this file tests SEARCH-06 requirement

    DO NOT implement actual test logic - this is Wave 0 stub creation.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=tenant-scoping</automated>
  </verify>
  <done>
    TenantScopingTest.php stub exists with 5 test method placeholders, proper namespace, and group annotation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Create feature test stub for async indexing</name>
  <files>tests/Feature/Search/AsyncIndexingTest.php</files>
  <action>
    Create tests/Feature/Search/AsyncIndexingTest.php:
    1. Namespace: Tests\Feature\Search
    2. Extend: Tests\TestCase
    3. Add setUp() method with placeholder for queue faking and job dispatch
    4. Add test method stubs (all marked with $this->assertTrue(true) to pass initially):
       - test_reindex_dispatch_returns_immediately()
       - test_reindex_creates_job_status()
       - test_reindex_job_indexes_all_products()
       - test_status_endpoint_returns_job_status()
       - test_concurrent_reindex_jobs_supported()
       - test_failed_indexing_jobs_retry()
    5. Add @group annotation for group=async-indexing
    6. Add docblock explaining this file tests QUEUE-07 requirement

    DO NOT implement actual test logic - this is Wave 0 stub creation.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Feature --group=async-indexing</automated>
  </verify>
  <done>
    AsyncIndexingTest.php stub exists with 6 test method placeholders, proper namespace, and group annotation.
  </done>
</task>

<task type="auto">
  <name>Task 6: Create Elasticsearch test factory in bootstrap</name>
  <files>tests/bootstrap.php</files>
  <action>
    Update tests/bootstrap.php to add Elasticsearch client factory:
    1. Check if file exists - if not, create it with <?php declaration
    2. Add helper function createElasticsearchClientForTests():
       - Reads ELASTICSEARCH_HOST and ELASTICSEARCH_PORT from env (defaults to localhost:9200)
       - Creates and returns Elasticsearch\Client instance with proper configuration
       - Throws exception if connection fails (fail fast in tests)
    3. Add helper function createTestIndex(string $indexName):
       - Uses client to create index with basic settings
       - Returns void
    4. Add helper function deleteTestIndex(string $indexName):
       - Uses client to delete index if exists
       - Returns void
    5. Add docblock explaining these helpers provide test isolation for Elasticsearch integration tests

    Use existing Elasticsearch container from Docker Compose. Do NOT create a separate test container.
  </action>
  <verify>
    <automated>php -r "require 'tests/bootstrap.php'; echo 'Bootstrap loaded successfully';"</automated>
  </verify>
  <done>
    tests/bootstrap.php updated with Elasticsearch client factory helpers for test isolation.
  </done>
</task>

</tasks>

<verification>
1. All 6 test stub files exist in correct directories
2. Each test stub has proper namespace extending TestCase
3. Each test stub has placeholder test methods with group annotations
4. tests/bootstrap.php provides Elasticsearch client factory
5. All tests pass with placeholder assertions (Wave 0 completion)
6. VALIDATION.md can be updated to nyquist_compliant: true
</verification>

<success_criteria>
1. ElasticsearchEngineTest.php stub exists with 6 test methods
2. SearchApiTest.php stub exists with 5 test methods
3. FuzzySearchTest.php stub exists with 5 test methods
4. TenantScopingTest.php stub exists with 5 test methods
5. AsyncIndexingTest.php stub exists with 6 test methods
6. tests/bootstrap.php provides Elasticsearch client helpers
7. All test files run without syntax errors
8. VALIDATION.md Wave 0 requirements satisfied
</success_criteria>

<output>
After completion, update `.planning/phases/05-elasticsearch-integration/05-VALIDATION.md` frontmatter to set `nyquist_compliant: true` and `wave_0_complete: true`, then create `.planning/phases/05-elasticsearch-integration/05-00-SUMMARY.md`
</output>
