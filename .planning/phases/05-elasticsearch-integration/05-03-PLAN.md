---
phase: 05-elasticsearch-integration
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/Jobs/IndexProductJob.php
  - app/Jobs/DeleteFromIndexJob.php
  - app/Jobs/ReindexTenantProductsJob.php
  - app/Http/Controllers/Api/V1/IndexController.php
  - app/Models/JobStatus.php
  - routes/api.php
  - tests/Feature/Search/AsyncIndexingTest.php
  - tests/Integration/Search/IndexOperationsTest.php
autonomous: true
requirements:
  - QUEUE-07
  - SEARCH-05
user_setup: []

must_haves:
  truths:
    - "Product indexing runs asynchronously in background queue"
    - "Index operations don't block HTTP responses (non-blocking)"
    - "Job status tracked for indexing operations (pending, running, completed, failed)"
    - "Agency admin can view queue job status in admin dashboard"
    - "Tenant context preserved in indexing jobs"
    - "Bulk indexing operations supported for catalog sync"
    - "Failed indexing jobs retry with exponential backoff"
  artifacts:
    - path: "app/Jobs/IndexProductJob.php"
      provides: "Async product indexing with tenant context"
      exports: ["__construct", "handle"]
      min_lines: 50
    - path: "app/Jobs/DeleteFromIndexJob.php"
      provides: "Async index deletion"
      exports: ["__construct", "handle"]
      min_lines: 40
    - path: "app/Jobs/ReindexTenantProductsJob.php"
      provides: "Bulk reindexing for catalog sync"
      exports: ["__construct", "handle"]
      min_lines: 60
    - path: "app/Http/Controllers/Api/V1/IndexController.php"
      provides: "Index management API endpoints"
      exports: ["reindex", "status"]
      min_lines: 60
    - path: "routes/api.php"
      provides: "Index management routes"
      contains: "Route::post('/reindex', [IndexController::class, 'reindex'])"
  key_links:
    - from: "app/Jobs/IndexProductJob.php"
      to: "app/Jobs/TenantAwareJob.php"
      via: "extends TenantAwareJob"
      pattern: "extends TenantAwareJob"
    - from: "app/Jobs/IndexProductJob.php"
      to: "app/Models/Product.php"
      via: "searchable() method"
      pattern: "->searchable\(\)"
    - from: "app/Http/Controllers/Api/V1/IndexController.php"
      to: "app/Jobs/ReindexTenantProductsJob.php"
      via: "job dispatch"
      pattern: "ReindexTenantProductsJob::dispatch"
    - from: "tests/Feature/Search/AsyncIndexingTest.php"
      to: "app/Http/Controllers/Api/V1/IndexController.php"
      via: "feature test coverage"
      pattern: "AsyncIndexingTest"
---

<objective>
Implement async product indexing with queue jobs for non-blocking search index updates and job status visibility.

Purpose: Enable background indexing of products to Elasticsearch without blocking HTTP requests, supporting bulk operations for catalog sync, and providing job status tracking for admin dashboard visibility.

Output: Tenant-aware indexing jobs (IndexProductJob, DeleteFromIndexJob, ReindexTenantProductsJob), IndexController for async index management, and comprehensive tests verifying async workflows and job status tracking.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-elasticsearch-integration/05-RESEARCH.md
@.planning/phases/05-elasticsearch-integration/05-01-PLAN.md
@.planning/phases/05-elasticsearch-integration/05-02-PLAN.md
@.planning/phases/04-background processing infrastructure/04-03-SUMMARY.md
@.planning/phases/04-background processing infrastructure/04-02-SUMMARY.md
@.planning/phases/03-tenant-management/03-03-SUMMARY.md

@app/Models/Product.php
@app/Models/Tenant.php
@app/Jobs/TenantAwareJob.php
@app/Services/QueueJobTracker.php
@app/Models/JobStatus.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IndexProductJob for async product indexing</name>
  <files>app/Jobs/IndexProductJob.php</files>
  <behavior>
    - Test 1: Job extends TenantAwareJob from Phase 4
    - Test 2: Job accepts product ID and tenant ID in constructor
    - Test 3: Job handle() method indexes product to Elasticsearch
    - Test 4: Job restores tenant context before indexing
    - Test 5: Job uses indexing queue (not default queue)
    - Test 6: Job implements exponential backoff (10s, 30s, 90s)
  </behavior>
  <action>
    Create app/Jobs/IndexProductJob.php:
    1. Extend App\Jobs\TenantAwareJob (from Phase 4)
    2. Constructor accepts string $productId, string $tenantId
    3. Store $this->tenantId for TenantAwareJob base class
    4. Set $this->queue = 'indexing'
    5. Set $this->tries = 3 and $this->timeout = 120
    6. handle() method:
      - Find product: $product = Product::findOrFail($this->productId)
      - Call $product->searchable() to index to Elasticsearch
      - Log indexing completion
    7. backoff() returns [10, 30, 90]

    Use TenantAwareJob pattern from Phase 4 for automatic tenant context restoration. Index to tenant's dedicated index.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=index-product-job</automated>
  </verify>
  <done>
    IndexProductJob extending TenantAwareJob with async indexing, tenant context restoration, and retry logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DeleteFromIndexJob for async index deletion</name>
  <files>app/Jobs/DeleteFromIndexJob.php</files>
  <behavior>
    - Test 1: Job extends TenantAwareJob
    - Test 2: Job accepts product ID and tenant ID in constructor
    - Test 3: Job handle() method removes product from Elasticsearch
    - Test 4: Job restores tenant context before deletion
    - Test 5: Job uses indexing queue
    - Test 6: Job implements exponential backoff
  </behavior>
  <action>
    Create app/Jobs/DeleteFromIndexJob.php:
    1. Extend App\Jobs\TenantAwareJob
    2. Constructor accepts string $productId, string $tenantId
    3. Store $this->tenantId for TenantAwareJob
    4. Set $this->queue = 'indexing'
    5. Set $this->tries = 3 and $this->timeout = 120
    6. handle() method:
      - Find product: $product = Product::findOrFail($this->productId)
      - Call $product->unsearchable() to remove from Elasticsearch
      - Log deletion completion
    7. backoff() returns [10, 30, 90]

    Follow same pattern as IndexProductJob. Use Scout's unsearchable() method for deletion.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=delete-from-index-job</automated>
  </verify>
  <done>
    DeleteFromIndexJob extending TenantAwareJob with async deletion and retry logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ReindexTenantProductsJob for bulk reindexing</name>
  <files>app/Jobs/ReindexTenantProductsJob.php</files>
  <behavior>
    - Test 1: Job extends TenantAwareJob
    - Test 2: Job accepts tenant ID in constructor
    - Test 3: Job deletes and recreates tenant index
    - Test 4: Job chunks products and indexes in batches of 100
    - Test 5: Job tracks progress with JobStatus
    - Test 6: Job handles large catalogs (1000+ products)
  </behavior>
  <action>
    Create app/Jobs/ReindexTenantProductsJob.php:
    1. Extend App\Jobs\TenantAwareJob
    2. Constructor accepts string $tenantId
    3. Store $this->tenantId for TenantAwareJob
    4. Set $this->queue = 'indexing'
    5. Set $this->tries = 1 (no retry for bulk operations)
    6. Set $this->timeout = 3600 (1 hour for large catalogs)
    7. handle() method:
      - Get tenant: $tenant = Tenant::currentTenant()
      - Create IndexManager instance
      - Delete tenant index: $indexManager->deleteIndex($tenant, 'products')
      - Recreate index: $indexManager->createIndex($tenant, 'products')
      - Chunk products by 100: $tenant->products()->chunk(100, function ($products) {
          foreach ($products as $product) {
            $product->searchable();
          }
        })
      - Log completion with product count

    Use IndexManager for index operations. Chunk products to avoid memory issues. Suitable for catalog sync from Phase 6.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=reindex-tenant-products-job</automated>
  </verify>
  <done>
    ReindexTenantProductsJob for bulk reindexing with chunked processing and index recreation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create IndexController for async index management</name>
  <files>app/Http/Controllers/Api/V1/IndexController.php, routes/api.php</files>
  <behavior>
    - Test 1: POST /api/v1/tenants/{tenantId}/reindex dispatches ReindexTenantProductsJob
    - Test 2: Reindex endpoint returns 202 Accepted with job_id
    - Test 3: Reindex endpoint creates JobStatus before dispatch
    - Test 4: GET /api/v1/jobs/{jobId}/status returns job status
    - Test 5: Status endpoint returns JobStatus with current state
    - Test 6: Both endpoints require authentication
  </behavior>
  <action>
    Create app/Http/Controllers/Api/V1/IndexController extending ApiController:
    1. Constructor accepts JobStatus model
    2. reindex(Request $request, string $tenantId): JsonResponse
       - Validate tenant exists: $tenant = Tenant::findOrFail($tenantId)
       - Create JobStatus with "pending" status, generate job_id (UUID), store payload
       - Dispatch ReindexTenantProductsJob::dispatch($tenantId)
       - Return response()->json(['data' => ['job_id' => $job->id, 'status' => 'pending', 'message' => 'Reindexing started'], 'meta' => []], 202)
    3. status(Request $request, string $jobId): JsonResponse
       - Find job: $job = JobStatus::where('id', $jobId)->firstOrFail()
       - Return $this->success(['job_id' => $job->id, 'status' => $job->status, 'payload' => $job->payload, 'error' => $job->error_message, 'created_at' => $job->created_at, 'updated_at' => $job->updated_at])
    4. Add routes:
       - Route::post('/tenants/{tenantId}/reindex', [IndexController::class, 'reindex'])->middleware(['auth:sanctum', 'throttle:api-write'])
       - Route::get('/jobs/{jobId}/status', [IndexController::class, 'status'])->middleware('auth:sanctum')

    Follow Phase 4 async pattern (202 Accepted, JobStatus tracking). Use JobStatus model from Phase 4 for QUEUE-07 requirement.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Feature --group=index-management</automated>
  </verify>
  <done>
    IndexController with async reindex endpoint, job status endpoint, authentication, and JobStatus tracking.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create async indexing integration tests</name>
  <files>tests/Feature/Search/AsyncIndexingTest.php</files>
  <behavior>
    - Test 1: Reindex dispatch returns immediately (< 100ms)
    - Test 2: Reindex creates JobStatus with pending status
    - Test 3: Job executes and indexes all tenant products
    - Test 4: Status endpoint returns current job status
    - Test 5: Multiple reindex jobs can run concurrently
    - Test 6: Failed indexing jobs retry with backoff
  </behavior>
  <action>
    Create tests/Feature/Search/AsyncIndexingTest.php:
    1. test_reindex_dispatch_returns_immediately() - call reindex endpoint, assert response time < 100ms
    2. test_reindex_creates_job_status() - call reindex endpoint, assert JobStatus created with status "pending"
    3. test_reindex_job_indexes_all_products() - create tenant with 10 products, dispatch reindex job, run queue worker, assert all products indexed
    4. test_status_endpoint_returns_job_status() - dispatch job, call status endpoint, assert returns correct status
    5. test_concurrent_reindex_jobs_supported() - dispatch 2 reindex jobs for different tenants, assert both execute
    6. test_failed_indexing_jobs_retry() - make indexing fail, assert job retries with exponential backoff

    Use Queue::fake() for most tests. Use real queue for integration test that verifies actual indexing. Measure response times.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Feature --group=async-indexing</automated>
  </verify>
  <done>
    Async indexing integration tests verifying non-blocking behavior, job status tracking, and retry logic.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Create index operations integration tests</name>
  <files>tests/Integration/Search/IndexOperationsTest.php</files>
  <behavior>
    - Test 1: IndexProductJob indexes product to correct tenant index
    - Test 2: DeleteFromIndexJob removes product from tenant index
    - Test 3: ReindexTenantProductsJob recreates index with all products
    - Test 4: Index operations preserve tenant context
    - Test 5: Bulk reindexing handles 1000+ products efficiently
  </behavior>
  <action>
    Create tests/Integration/Search/IndexOperationsTest.php:
    1. test_index_product_job_indexes_to_tenant_index() - dispatch IndexProductJob, run worker, verify product in tenant's Elasticsearch index
    2. test_delete_from_index_job_removes_product() - index product, dispatch DeleteFromIndexJob, verify removed from index
    3. test_reindex_tenant_products_job_recreates_index() - create tenant with products, dispatch ReindexTenantProductsJob, verify index recreated with all products
    4. test_index_operations_preserve_tenant_context() - verify tenant context set during job execution
    5. test_bulk_reindexing_handles_large_catalogs() - create 1000 products, dispatch reindex job, verify all indexed without errors

    Use real Elasticsearch client. Use real queue worker (not Queue::fake()). Verify actual index state.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=index-operations</automated>
  </verify>
  <done>
    Index operations integration tests verifying job execution, tenant context, and bulk processing.
  </done>
</task>

</tasks>

<verification>
1. IndexProductJob indexes products asynchronously to tenant indices
2. DeleteFromIndexJob removes products from indices asynchronously
3. ReindexTenantProductsJob handles bulk reindexing with chunking
4. IndexController returns 202 Accepted for async operations
5. Job status tracked and queryable via status endpoint
6. Async operations don't block HTTP responses (< 100ms)
7. Tenant context preserved in all indexing jobs
8. Failed jobs retry with exponential backoff
9. Integration tests verify complete async workflows
10. Job status model provides visibility for admin dashboard (QUEUE-07)
</verification>

<success_criteria>
1. Product indexing runs asynchronously in background queue (non-blocking)
2. Index operations tracked with JobStatus model (pending → running → completed/failed)
3. Agency admin can view queue job status via API endpoint (QUEUE-07)
4. Tenant context preserved in indexing jobs
5. Bulk reindexing supported for catalog sync (SEARCH-05)
6. Failed indexing jobs retry with exponential backoff
7. All jobs use indexing queue (separate from default queue)
8. Integration tests verify async workflows with real queue and Elasticsearch
</success_criteria>

<output>
After completion, create `.planning/phases/05-elasticsearch-integration/05-03-SUMMARY.md`
</output>
