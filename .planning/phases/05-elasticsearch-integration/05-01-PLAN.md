---
phase: 05-elasticsearch-integration
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified:
  - composer.json
  - .env.docker
  - config/scout.php
  - app/Engines/ElasticsearchEngine.php
  - app/Search/IndexManager.php
  - app/Models/Product.php
  - database/migrations/2026_03_13_000001_create_products_table.php
  - tests/Unit/Search/ElasticsearchEngineTest.php
  - tests/Integration/Search/IndexManagementTest.php
autonomous: true
requirements:
  - SEARCH-05
  - SEARCH-06
  - SEARCH-07
user_setup: []

must_haves:
  truths:
    - "Product searches execute successfully against Elasticsearch"
    - "Each tenant's products stored in dedicated Elasticsearch index (products_tenant_{id})"
    - "Products can be indexed to Elasticsearch via Scout's searchable() method"
    - "Products can be removed from Elasticsearch index via unsearchable() method"
    - "Search operations for one tenant never return results from other tenants (complete isolation)"
    - "Search results include relevant matches for typos and partial text"
  artifacts:
    - path: "app/Engines/ElasticsearchEngine.php"
      provides: "Custom Scout engine for ES 8.x with multi-tenant support"
      exports: ["update", "delete", "search", "map", "getTotalCount", "flush"]
      min_lines: 150
    - path: "app/Search/IndexManager.php"
      provides: "Index-per-tenant management service"
      exports: ["getIndexName", "createIndex", "deleteIndex", "getIndexSettings", "getProductMappings"]
      min_lines: 100
    - path: "app/Models/Product.php"
      provides: "Product model with Searchable trait and tenant-aware indexing"
      contains: "use Searchable"
      min_lines: 80
    - path: "database/migrations/2026_03_13_000001_create_products_table.php"
      provides: "Products table with tenant_id foreign key"
      contains: "tenant_id"
      min_lines: 40
    - path: "config/scout.php"
      provides: "Scout configuration with custom driver"
      contains: "SCOUT_DRIVER=custom"
    - path: "composer.json"
      provides: "Elasticsearch and Scout dependencies"
      contains: "laravel/scout", "elasticsearch/elasticsearch"
  key_links:
    - from: "app/Models/Product.php"
      to: "app/Search/IndexManager.php"
      via: "searchableAs() method"
      pattern: "getIndexName\(Tenant\)"
    - from: "app/Engines/ElasticsearchEngine.php"
      to: "elasticsearch/elasticsearch client"
      via: "constructor injection"
      pattern: "Client \$client"
    - from: "app/Engines/ElasticsearchEngine.php"
      to: "app/Search/IndexManager.php"
      via: "IndexManager dependency"
      pattern: "IndexManager \$indexManager"
    - from: "tests/Unit/Search/ElasticsearchEngineTest.php"
      to: "app/Engines/ElasticsearchEngine.php"
      via: "unit test coverage"
      pattern: "ElasticsearchEngineTest"
---

<objective>
Build Elasticsearch engine and index management infrastructure with index-per-tenant strategy for complete data isolation.

Purpose: Enable sub-second full-text search across product catalogs with strict tenant isolation using Elasticsearch 8.x and Laravel Scout. This foundation supports fuzzy matching, pagination, and async indexing in subsequent plans.

Output: Custom ElasticsearchEngine, IndexManager service for multi-tenant index management, Product model with Searchable trait, and comprehensive test coverage verifying tenant isolation.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-elasticsearch-integration/05-RESEARCH.md
@.planning/phases/05-elasticsearch-integration/05-00-PLAN.md
@.planning/phases/04-background processing infrastructure/04-03-SUMMARY.md
@.planning/phases/03-tenant-management/03-03-SUMMARY.md

@app/Models/Tenant.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Scout and Elasticsearch dependencies</name>
  <files>composer.json</files>
  <behavior>
    - Test 1: Composer require laravel/scout:^10.0 succeeds
    - Test 2: Composer require elasticsearch/elasticsearch:^8.13 succeeds
    - Test 3: Scout config file exists after vendor:publish
    - Test 4: SCOUT_DRIVER environment variable can be set
  </behavior>
  <action>
    Install Scout and Elasticsearch PHP client:
    1. Run `composer require laravel/scout:^10.0`
    2. Run `composer require elasticsearch/elasticsearch:^8.13`
    3. Run `php artisan vendor:publish --provider="Laravel\Scout\ScoutServiceProvider"`
    4. Add to .env.docker: SCOUT_DRIVER=custom, ELASTICSEARCH_HOST=elasticsearch, ELASTICSEARCH_PORT=9200
    5. Update config/scout.php to set driver to env('SCOUT_DRIVER', 'custom')

    Do NOT create any custom engine code yet - this task is just dependency installation and configuration.
  </action>
  <verify>
    <automated>composer show laravel/scout elasticsearch/elasticsearch && grep -q "SCOUT_DRIVER" .env.docker && test -f config/scout.php</automated>
  </verify>
  <done>
    Scout 10.x and Elasticsearch PHP client 8.x installed, config published, environment variables configured.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create IndexManager service for tenant-scoped indices</name>
  <files>app/Search/IndexManager.php</files>
  <behavior>
    - Test 1: getIndexName() returns "products_tenant_{uuid}" for product type
    - Test 2: createIndex() creates index with correct settings and mappings
    - Test 3: getProductMappings() includes fuzzy matching analyzer on name field
    - Test 4: getIndexSettings() configures product_analyzer with lowercase and asciifolding
    - Test 5: deleteIndex() removes index from Elasticsearch
  </behavior>
  <action>
    Create app/Search/IndexManager.php service:
    1. Constructor accepts Elasticsearch\Client client
    2. getIndexName(Tenant $tenant, string $type = 'products'): string - returns "{type}_tenant_{tenant->id}"
    3. createIndex(Tenant $tenant, string $type = 'products'): void - creates index with settings and mappings
    4. deleteIndex(Tenant $tenant, string $type = 'products'): void - deletes index
    5. getIndexSettings(): array - returns number_of_shards: 1, number_of_replicas: 0, analysis with product_analyzer (standard tokenizer, lowercase + asciifolding filters)
    6. getProductMappings(): array - returns properties for id (keyword), tenant_id (keyword), name (text with analyzer, keyword sub-field), description (text with analyzer), sku (keyword), price (double), stock (integer), status (keyword), created_at/updated_at (date)

    Use index-per-tenant strategy: each tenant gets dedicated index for complete data isolation. Configure custom analyzer for fuzzy matching support.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=index-manager</automated>
  </verify>
  <done>
    IndexManager service with tenant-scoped index operations, fuzzy matching analyzer configuration, and comprehensive unit tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create custom ElasticsearchEngine for Scout</name>
  <files>app/Engines/ElasticsearchEngine.php</files>
  <behavior>
    - Test 1: update() indexes models to correct tenant index
    - Test 2: delete() removes models from correct tenant index
    - Test 3: search() queries only tenant's index
    - Test 4: buildQuery() creates multi_match query with fuzziness AUTO
    - Test 5: map() returns Collection of models ordered by relevance
    - Test 6: getTotalCount() returns total hits from search results
  </behavior>
  <action>
    Create app/Engines/ElasticsearchEngine.php extending Laravel\Scout\Engines\Engine:
    1. Constructor accepts Client $client, IndexManager $indexManager
    2. update($models): void - bulk index models to their tenant indices using bulk API
    3. delete($models): void - bulk delete models from their tenant indices
    4. search(Builder $builder): array - search current tenant's index with multi_match query
    5. buildQuery(Builder $builder): array - bool query with multi_match on name^2, description, sku fields, fuzziness: AUTO, prefix_length: 2
    6. mapIds($results): array - extract document IDs from hits
    7. map(Builder $builder, $results, $model): Collection - map hits to model instances, filter by found IDs
    8. getTotalCount($results): int - return total hits value
    9. flush($model): void - delete tenant's entire product index
    10. getTenantForModel(Model $model): Tenant - return $model->tenant ?? Tenant::currentTenant()

    Implement ES 8.x compatible queries with fuzzy matching. Always scope to current tenant's index.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=elasticsearch-engine</automated>
  </verify>
  <done>
    Custom ElasticsearchEngine implementing all Scout engine methods with ES 8.x compatibility and tenant-aware index routing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create Product model and migration with Searchable trait</name>
  <files>app/Models/Product.php, database/migrations/2026_03_13_000001_create_products_table.php</files>
  <behavior>
    - Test 1: Product migration creates products table with tenant_id foreign key
    - Test 2: Product model uses Searchable trait
    - Test 3: searchableAs() returns tenant-scoped index name
    - Test 4: toSearchArray() returns all searchable fields as array
    - Test 5: shouldBeSearchable() returns true only for active products
    - Test 6: products() relationship exists on Tenant model
  </behavior>
  <action>
    Create database migration and Product model:
    1. Migration: create_products_table with id (uuid primary), tenant_id (foreign key), name (string), description (text), sku (string), price (decimal 10,2), stock (integer), status (enum: active, inactive), timestamps, foreign key constraint on tenant_id
    2. Product model with HasFactory, HasUuids, Searchable traits
    3. $fillable = ['tenant_id', 'name', 'description', 'sku', 'price', 'stock', 'status']
    4. $casts = ['price' => 'decimal:2', 'stock' => 'integer']
    5. tenant(): BelongsTo relationship to Tenant
    6. searchableAs(): string - return "products_tenant_{$this->tenant_id}"
    7. toSearchArray(): array - return id, tenant_id, name, description, sku, price, stock, status, created_at, updated_at
    8. shouldBeSearchable(): bool - return $this->status === 'active'
    9. Add products() HasMany relationship to Tenant model

    Run migration after creating files.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Unit --group=product-model && php artisan migrate:status | grep products</automated>
  </verify>
  <done>
    Product model with Searchable trait, tenant-scoped indexing, products migration created and run, Tenant relationship added.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Register ElasticsearchEngine and create integration tests</name>
  <files>app/Providers/AppServiceProvider.php, tests/Integration/Search/IndexManagementTest.php</files>
  <behavior>
    - Test 1: Scout uses custom ElasticsearchEngine driver
    - Test 2: Integration test creates tenant index successfully
    - Test 3: Integration test indexes product to tenant index
    - Test 4: Integration test deletes product from tenant index
    - Test 5: Integration test verifies tenant isolation (Product from Tenant A not in Tenant B index)
  </behavior>
  <action>
    1. Register Scout engine in AppServiceProvider boot():
   - Use Laravel\Scout\EngineManager
   - $manager->extend('custom', fn() => new ElasticsearchEngine(app('elasticsearch'), app(IndexManager::class)))
   - Bind Elasticsearch client: app()->singleton('elasticsearch', fn() => Client::create(['hosts' => [env('ELASTICSEARCH_HOST', 'localhost') . ':' . env('ELASTICSEARCH_PORT', '9200')]]))
   - Bind IndexManager: app()->singleton(IndexManager::class, fn() => new IndexManager(app('elasticsearch')))

2. Create tests/Integration/Search/IndexManagementTest.php:
   - test_create_tenant_index_creates_index_with_mappings()
   - test_index_product_to_tenant_index()
   - test_delete_product_from_tenant_index()
   - test_tenant_isolation_prevents_cross_tenant_leakage() - create 2 tenants, index product for tenant A, verify not in tenant B index

Use real Elasticsearch client in integration tests. Use existing ES container from Docker.
  </action>
  <verify>
    <automated>./vendor/bin/pest --testsuites=Integration --group=index-management</automated>
  </verify>
  <done>
    ElasticsearchEngine registered with Scout, IndexManager singleton bound, integration tests verifying tenant isolation and index operations.
  </done>
</task>

</tasks>

<verification>
1. All unit tests for ElasticsearchEngine pass (search, index, delete operations)
2. All unit tests for IndexManager pass (index naming, creation, deletion)
3. Product model tests pass (Searchable trait, tenant-scoped index name)
4. Integration tests verify tenant isolation (no cross-tenant data leakage)
5. Scout custom engine registered and functional
6. Elasticsearch indices created per tenant with correct mappings
7. Products can be indexed and removed from Elasticsearch
8. Index mappings support fuzzy matching with product_analyzer
</verification>

<success_criteria>
1. Product searches execute successfully against Elasticsearch
2. Each tenant gets dedicated "products_tenant_{id}" index
3. Product model integrates with Scout Searchable trait
4. Products can be indexed to tenant-specific indices
5. Products can be deleted from tenant-specific indices
6. Index mappings include fuzzy matching analyzer on text fields
7. Integration tests verify complete tenant data isolation
8. Custom ElasticsearchEngine implements all required Scout methods
9. IndexManager service handles all index CRUD operations
10. All tests pass with >90% code coverage for engine and index manager
</success_criteria>

<output>
After completion, create `.planning/phases/05-elasticsearch-integration/05-01-SUMMARY.md`
</output>
