---
phase: 11-interactive-api-documentation
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified:
  - public/docs/index.html
  - tests/Feature/ScribeGenerationTest.php
  - tests/Feature/DocumentationEndpointTest.php
  - tests/Feature/EndpointCoverageTest.php
  - tests/Feature/CurlCommandsTest.php
  - tests/Feature/ResponseSchemaTest.php
autonomous: false
requirements:
  - APIDOCS-01
  - APIDOCS-02
  - APIDOCS-03
  - APIDOCS-04
  - APIDOCS-05

must_haves:
  truths:
    - "Documentation regenerated with all controller annotations"
    - "All 18 API endpoints appear in generated documentation"
    - "Endpoints grouped into 5 logical groups"
    - "Authenticated endpoints show 'Authenticated' badge"
    - "Each endpoint has curl command example"
    - "Response schemas documented with field types"
    - "Interactive 'Try it out' buttons work with Sanctum authentication"
    - "All Wave 0 tests pass with real assertions (GREEN phase)"
  artifacts:
    - path: "public/docs/index.html"
      provides: "Complete API documentation site"
      min_lines: 500
    - path: "tests/Feature/ScribeGenerationTest.php"
      provides: "Assertions for Scribe generation"
      contains: "assertFileExists, assertStringContainsString"
    - path: "tests/Feature/DocumentationEndpointTest.php"
      provides: "Assertions for /docs endpoint"
      contains: "assertStatus, assertSee"
    - path: "tests/Feature/EndpointCoverageTest.php"
      provides: "Assertions for endpoint coverage"
      contains: "assertCount, assertStringContainsString"
    - path: "tests/Feature/CurlCommandsTest.php"
      provides: "Assertions for curl commands"
      contains: "assertStringContainsString('curl')"
    - path: "tests/Feature/ResponseSchemaTest.php"
      provides: "Assertions for response schemas"
      contains: "assertStringContainsString('responseField')"
  key_links:
    - from: "tests/Feature/*Test.php"
      to: "public/docs/index.html"
      via: " assertions verify generated content"
      pattern: "tests validate documentation quality"
    - from: "public/docs/index.html"
      to: "app/Http/Controllers/Api/V1/*.php"
      via: "Scribe reads docblocks"
      pattern: "documentation reflects code annotations"
    - from: "/docs endpoint"
      to: "employer portfolio review"
      via: "publicly accessible documentation"
      pattern: "portfolio-ready API demonstration"
---

<objective>
Regenerate complete documentation and convert Wave 0 tests to real assertions (GREEN phase).

Purpose: Generate final comprehensive API documentation with all annotations from Plan 11-02, then verify quality through automated tests. This completes the TDD cycle by replacing placeholder assertions with real validation. Employer can review beautiful, interactive API documentation at /docs without reading source code.

Output: Complete generated documentation site at public/docs/ with all endpoints documented, tested, and working interactive examples.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-interactive-api-documentation/11-RESEARCH.md
@.planning/phases/11-interactive-api-documentation/11-02-SUMMARY.md
@.planning/phases/11-interactive-api-documentation/11-01-SUMMARY.md
@.planning/phases/11-interactive-api-documentation/11-00-SUMMARY.md

# TDD Pattern Reference (from Phase 09-00 in STATE.md)
RED Phase (Plan 11-00): Create test stubs with assertTrue(true)
GREEN Phase (This Plan): Implement real assertions that pass
REFACTOR Phase (if needed): Clean up and improve

# Existing Test Structure (from 11-00-PLAN.md)
5 test files with placeholder assertions:
- ScribeGenerationTest.php: 3 tests
- DocumentationEndpointTest.php: 3 tests
- EndpointCoverageTest.php: 4 tests
- CurlCommandsTest.php: 4 tests
- ResponseSchemaTest.php: 4 tests

Total: 18 placeholder assertions to convert to real tests

# Expected Documentation Content (from 11-02-SUMMARY.md)
5 endpoint groups:
1. Authentication (4 endpoints: register, login, logout, me)
2. Tenant Management (5 endpoints: index, store, show, update, destroy)
3. Catalog Synchronization (3 endpoints: dispatch, status, history)
4. Product Search (3 endpoints: search, status, reindex)
5. Index Management (3 endpoints: reindex, status, list)

Total: 18 endpoints to document

# 11-RESEARCH.md Pitfall 3: Forgetting to Regenerate Documentation
"What goes wrong: Documentation shows outdated endpoints or missing new features"
"How to avoid: Add documentation generation to deployment script (Phase 10) and CI/CD pipeline"

This plan addresses this pitfall by regenerating after annotations and documenting the process.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate documentation with all controller annotations</name>
  <files>public/docs/index.html</files>
  <action>
    Regenerate complete API documentation:

    - Run: php artisan scribe:generate
    - Expected output: "Generated API documentation successfully!"
    - Verify: No warnings or errors during generation
    - Result: public/docs/ directory contains complete static site

    What Scribe does:
    - Reads all API routes from routes/api.php
    - Parses @group, @authenticated, @bodyParam, @responseField, @response annotations
    - Generates HTML with navigation, examples, and interactive "Try it out"
    - Creates curl commands automatically for each endpoint
    - Organizes endpoints by @group annotations

    Expected documentation structure:
    - Home page with overview
    - 5 endpoint groups (Authentication, Tenant Management, Catalog Synchronization, Product Search, Index Management)
    - Each endpoint shows: HTTP method, path, description, authentication badge, parameters, response schema, example curl command
    - Navigation sidebar for quick access
    - Search functionality
    - "Try it out" button (injects Sanctum token via test user)
  </action>
  <verify>
    <automated>test -f public/docs/index.html && test -s public/docs/index.html && echo "Documentation regenerated"</automated>
  </verify>
  <done>public/docs/index.html generated with all 18 endpoints documented, no errors in generation output</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete API documentation site generated at public/docs/index.html with:
    - 5 endpoint groups (Authentication, Tenant Management, Catalog Synchronization, Product Search, Index Management)
    - All 18 API endpoints documented with descriptions, parameters, response schemas
    - curl command examples for each endpoint
    - Interactive "Try it out" buttons
    - Navigation and search functionality
  </what-built>
  <how-to-verify>
    1. Start Docker containers: make up
    2. Visit http://localhost/docs in browser
    3. Verify documentation loads without errors
    4. Check "Authentication" group appears in navigation
    5. Click on "POST /api/v1/register" endpoint
    6. Verify: HTTP method, path, description shown
    7. Verify: Request parameters listed (name, email, password) with types
    8. Verify: Response example shows JSON with user and token
    9. Verify: curl command example displayed
    10. Scroll down to "Tenant Management" group
    11. Click on "GET /api/v1/tenants" endpoint
    12. Verify: "Authenticated" badge visible
    13. Verify: Response fields documented (id, name, slug, status, platform_type)
    14. Click "Try it out" button (if visible)
    15. Verify: Can execute request and see response (test user auto-authenticated)
    16. Check search functionality: search for "sync" in search box
    17. Verify: "Catalog Synchronization" group appears in results
  </how-to-verify>
  <resume-signal>
    Type "approved" if documentation looks complete and professional, or describe issues to fix.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement real assertions in ScribeGenerationTest.php (GREEN phase)</name>
  <files>tests/Feature/ScribeGenerationTest.php</files>
  <action>
    Replace placeholder assertions with real tests in ScribeGenerationTest.php:

    1. test_scribe_generates_documentation():
       - Run: php artisan scribe:generate
       - Assert: File exists at public/docs/index.html
       - Assert: File is not empty (size > 0)
       - Assert: File contains HTML doctype

    2. test_documentation_includes_all_endpoints():
       - Read public/docs/index.html content
       - Assert: Contains "Authentication" group
       - Assert: Contains "Tenant Management" group
       - Assert: Contains "Catalog Synchronization" group
       - Assert: Contains "Product Search" group
       - Assert: Contains "Index Management" group
       - Assert: Contains all 18 endpoint paths (e.g., "/api/v1/register", "/api/v1/tenants", "/api/v1/sync/dispatch")

    3. test_generated_html_is_valid():
       - Read public/docs/index.html content
       - Assert: Contains "<!DOCTYPE html>"
       - Assert: Contains "<html>" tag
       - Assert: Contains closing "</html>" tag
       - Assert: Contains "API Documentation" heading

    Replace all assertTrue(true) with actual assertions that verify Scribe generation works correctly.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/ScribeGenerationTest.php</automated>
  </verify>
  <done>All 3 tests in ScribeGenerationTest.php pass with real assertions (no placeholders)</done>
</task>

<task type="auto">
  <name>Task 3: Implement real assertions in DocumentationEndpointTest.php (GREEN phase)</name>
  <files>tests/Feature/DocumentationEndpointTest.php</files>
  <action>
    Replace placeholder assertions with real tests in DocumentationEndpointTest.php:

    1. test_docs_endpoint_returns_200():
       - Make GET request to /docs
       - Assert: Response status is 200
       - Assert: Response is successful

    2. test_docs_returns_html_content():
       - Make GET request to /docs
       - Assert: Response header Content-Type contains "text/html"
       - Assert: Response content is not empty

    3. test_html_includes_documentation_heading():
       - Make GET request to /docs
       - Assert: Response content contains "API Documentation"
       - Assert: Response content contains "Authentication" (first group)
       - Assert: Response content contains navigation elements

    These tests verify the /docs route serves the generated documentation correctly (APIDOCS-02).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/DocumentationEndpointTest.php</automated>
  </verify>
  <done>All 3 tests in DocumentationEndpointTest.php pass with real assertions (no placeholders)</done>
</task>

<task type="auto">
  <name>Task 4: Implement real assertions in EndpointCoverageTest.php (GREEN phase)</name>
  <files>tests/Feature/EndpointCoverageTest.php</files>
  <action>
    Replace placeholder assertions with real tests in EndpointCoverageTest.php:

    1. test_all_api_routes_documented():
       - Get all API routes from routes/api.php (use Route::getRoutes())
       - Filter for API v1 routes with prefix 'api/v1'
       - Count expected routes (should be 18)
       - Read public/docs/index.html content
       - Assert: Each route path appears in documentation
       - Assert: Documentation contains "18" endpoints (or actual count)

    2. test_endpoints_grouped_by_controller():
       - Read public/docs/index.html content
       - Assert: Contains "Authentication" heading (group)
       - Assert: Contains "Tenant Management" heading (group)
       - Assert: Contains "Catalog Synchronization" heading (group)
       - Assert: Contains "Product Search" heading (group)
       - Assert: Contains "Index Management" heading (group)

    3. test_authenticated_endpoints_show_badge():
       - Read public/docs/index.html content
       - Assert: Contains "Authenticated" text or badge class
       - Authenticated endpoints: login, logout, me, all tenant routes (except index/store), all sync/search/index routes
       - Assert: At least 14 endpoints show "Authenticated" badge

    4. test_tenant_scoped_endpoints_document_header():
       - Read public/docs/index.html content
       - Assert: Contains "X-Tenant-ID" header documentation
       - Tenant-scoped endpoints: show, update, destroy (tenants), history (sync)
       - Assert: Header appears in relevant endpoint documentation

    These tests verify comprehensive endpoint coverage (APIDOCS-03).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/EndpointCoverageTest.php</automated>
  </verify>
  <done>All 4 tests in EndpointCoverageTest.php pass with real assertions (no placeholders)</done>
</task>

<task type="auto">
  <name>Task 5: Implement real assertions in CurlCommandsTest.php (GREEN phase)</name>
  <files>tests/Feature/CurlCommandsTest.php</files>
<action>
    Replace placeholder assertions with real tests in CurlCommandsTest.php:

    1. test_endpoints_have_curl_examples():
       - Read public/docs/index.html content
       - Assert: Contains "curl" command text
       - Assert: Contains multiple curl examples (at least 10)
       - Assert: curl examples appear in endpoint sections

    2. test_curl_commands_use_correct_http_method():
       - Read public/docs/index.html content
       - Assert: Contains "curl -X POST" (for POST endpoints)
       - Assert: Contains "curl -X GET" (for GET endpoints)
       - Assert: Contains "curl -X PUT" (for PUT endpoints)
       - Assert: Contains "curl -X DELETE" (for DELETE endpoints)

    3. test_authenticated_commands_include_authorization_header():
       - Read public/docs/index.html content
       - Assert: Contains 'Authorization: Bearer' in curl examples
       - Assert: Bearer token appears in authenticated endpoint examples
       - Assert: Token placeholder format is correct (e.g., "{your-token}" or actual test token)

    4. test_request_examples_match_validation_rules():
       - For Auth endpoints: Assert curl examples include name, email, password fields
       - For Tenant endpoints: Assert examples include name, platform_type, api_credentials
       - For Sync endpoints: Assert examples include tenant_id, sync_type
       - Verify request body examples match FormRequest validation rules

    These tests verify curl command examples (APIDOCS-04).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/CurlCommandsTest.php</automated>
  </verify>
  <done>All 4 tests in CurlCommandsTest.php pass with real assertions (no placeholders)</done>
</task>

<task type="auto">
  <name>Task 6: Implement real assertions in ResponseSchemaTest.php (GREEN phase)</name>
  <files>tests/Feature/ResponseSchemaTest.php</files>
  <action>
    Replace placeholder assertions with real tests in ResponseSchemaTest.php:

    1. test_response_fields_have_data_types():
       - Read public/docs/index.html content
       - Assert: Contains field type annotations (e.g., "string", "integer", "array", "boolean")
       - Assert: Types appear in response field documentation
       - Assert: At least 20 fields have type documentation

    2. test_response_field_annotations_present():
       - For User fields: Assert "id", "name", "email" documented
       - For Tenant fields: Assert "id", "name", "slug", "status", "platform_type" documented
       - For SyncLog fields: Assert "job_id", "status", "progress", "products_indexed" documented
       - For Product fields: Assert "id", "name", "price", "stock_status" documented
       - Verify @responseField annotations appear in docs

    3. test_example_responses_show_json_structure():
       - Read public/docs/index.html content
       - Assert: Contains JSON response examples
       - Assert: Examples show "data" and "meta" structure
       - Assert: Examples show nested objects and arrays
       - Assert: Examples are valid JSON format

    4. test_error_responses_documented():
       - Read public/docs/index.html content
       - Assert: Contains "401" error response examples
       - Assert: Contains "422" error response examples (validation errors)
       - Assert: Contains "404" error response examples
       - Assert: Error responses show "errors" array structure with field and message

    These tests verify response schema documentation (APIDOCS-05).
  </action>
  <verify>
    <automated>php artisan test tests/Feature/ResponseSchemaTest.php</automated>
  </verify>
  <done>All 4 tests in ResponseSchemaTest.php pass with real assertions (no placeholders)</done>
</task>

<task type="auto">
  <name>Task 7: Run full test suite and verify all GREEN phase tests pass</name>
  <files>tests/Feature/ScribeGenerationTest.php, tests/Feature/DocumentationEndpointTest.php, tests/Feature/EndpointCoverageTest.php, tests/Feature/CurlCommandsTest.php, tests/Feature/ResponseSchemaTest.php</files>
  <action>
    Run complete test suite for Phase 11:

    - Run: php artisan test tests/Feature/ScribeGenerationTest.php
    - Run: php artisan test tests/Feature/DocumentationEndpointTest.php
    - Run: php artisan test tests/Feature/EndpointCoverageTest.php
    - Run: php artisan test tests/Feature/CurlCommandsTest.php
    - Run: php artisan test tests/Feature/ResponseSchemaTest.php

    Or run all Phase 11 tests together:
    - Run: php artisan test tests/Feature/*Test.php --filter=Scribe|Documentation|Endpoint|Curl|Response

    Verify:
    - All 18 tests pass (3+3+4+4+4)
    - No placeholder assertions remain (all assertTrue(true) replaced)
    - Total assertions: Should be 30+ (each test has multiple assertions)
    - Test execution time: < 60 seconds

    This completes the TDD GREEN phase - implementation matches test requirements.
  </action>
  <verify>
    <automated>php artisan test tests/Feature/ScribeGenerationTest.php tests/Feature/DocumentationEndpointTest.php tests/Feature/EndpointCoverageTest.php tests/Feature/CurlCommandsTest.php tests/Feature/ResponseSchemaTest.php</automated>
  </verify>
  <done>All 18 tests pass with real assertions, no placeholders remaining, TDD GREEN phase complete</done>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] Documentation regenerated successfully (public/docs/index.html created)
- [ ] All 18 endpoints appear in generated documentation
- [ ] 5 endpoint groups visible in navigation
- [ ] Authenticated endpoints show "Authenticated" badge
- [ ] curl command examples present for all endpoints
- [ ] Response schemas documented with field types
- [ ] Interactive "Try it out" buttons work (manual checkpoint verified)
- [ ] All 18 tests pass with real assertions (no placeholders)
- [ ] Test coverage: 18 tests, 30+ assertions
- [ ] TDD cycle complete: RED (11-00) → GREEN (11-03)
</verification>

<success_criteria>
1. Complete API documentation generated at public/docs/index.html
2. All 18 endpoints documented with descriptions, parameters, response schemas
3. Documentation organized into 5 logical groups with navigation
4. curl command examples available for all endpoints
5. Response schemas documented with field types and examples
6. Interactive "Try it out" functionality works with Sanctum authentication
7. All Wave 0 tests converted to real assertions and passing
8. Portfolio-ready: Employer can visit /docs and see full API capabilities without reading code
9. DOITSUYA criteria met: "API-first backend system" with visible, consumable API
</success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-api-documentation/11-03-SUMMARY.md`
</output>
