---
phase: 11-interactive-api-documentation
plan: 00
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/Feature/ScribeGenerationTest.php
  - tests/Feature/DocumentationEndpointTest.php
  - tests/Feature/EndpointCoverageTest.php
  - tests/Feature/CurlCommandsTest.php
  - tests/Feature/ResponseSchemaTest.php
autonomous: true
requirements:
  - APIDOCS-01
  - APIDOCS-02
  - APIDOCS-03
  - APIDOCS-04
  - APIDOCS-05

must_haves:
  truths:
    - "All test files exist with placeholder assertions (Nyquist compliance)"
    - "Tests cover Scribe generation, documentation endpoint, endpoint coverage, curl commands, and response schemas"
    - "Test structure follows Phase 10 TDD patterns (RED phase with assertTrue(true))"
  artifacts:
    - path: "tests/Feature/ScribeGenerationTest.php"
      provides: "Test Scribe package generates documentation from code"
      min_lines: 30
    - path: "tests/Feature/DocumentationEndpointTest.php"
      provides: "Test /docs endpoint serves HTML documentation"
      min_lines: 25
    - path: "tests/Feature/EndpointCoverageTest.php"
      provides: "Test all API endpoints are documented"
      min_lines: 40
    - path: "tests/Feature/CurlCommandsTest.php"
      provides: "Test documentation includes curl examples"
      min_lines: 30
    - path: "tests/Feature/ResponseSchemaTest.php"
      provides: "Test response schemas are documented"
      min_lines: 35
  key_links:
    - from: "tests/Feature/ScribeGenerationTest.php"
      to: "knuckleswtf/scribe"
      via: "composer require --dev knuckleswtf/scribe"
      pattern: "scribe:generate command"
    - from: "tests/Feature/DocumentationEndpointTest.php"
      to: "routes/web.php"
      via: "/docs route"
      pattern: "Route::view('/docs', 'scribe::index')"
    - from: "tests/Feature/EndpointCoverageTest.php"
      to: "app/Http/Controllers/Api/V1/*.php"
      via: "@group and @authenticated annotations"
      pattern: "all controllers have docblocks"
---

<objective>
Create test stubs for all API documentation requirements following TDD RED phase pattern.

Purpose: Establish test structure first (RED phase) before implementing Scribe documentation system. These tests define what "complete documentation" means and will drive implementation in subsequent waves.

Output: 5 test files with placeholder assertions (assertTrue(true)) that will be implemented in later waves.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-interactive-api-documentation/11-RESEARCH.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Existing Test Patterns from Phase 09-00
From tests/Feature/Export/SyncLogExportTest.php:
```php
class SyncLogExportTest extends TestCase
{
    use RefreshDatabase;

    public function test_sync_log_export_dispatches_job(): void
    {
        $this->assertTrue(true); // RED phase placeholder
    }
}
```

From Phase 10 TDD pattern (STATE.md):
- "TDD Wave 0 pattern: Placeholder assertions ($this->assertTrue(true)) for Nyquist compliance"
- "Test organization: 4 feature test files (integration) + 1 unit test file (service logic)"
</context>

<feature>
  <name>API Documentation Test Suite</name>
  <files>
    - tests/Feature/ScribeGenerationTest.php
    - tests/Feature/DocumentationEndpointTest.php
    - tests/Feature/EndpointCoverageTest.php
    - tests/Feature/CurlCommandsTest.php
    - tests/Feature/ResponseSchemaTest.php
  </files>
  <behavior>
    Test 1 (ScribeGeneration): Scribe package generates documentation from code
    - When: php artisan scribe:generate is executed
    - Then: public/docs directory is created with index.html
    - And: Documentation includes all API endpoints grouped by controller

    Test 2 (DocumentationEndpoint): /docs endpoint serves HTML documentation
    - When: GET request to /docs
    - Then: Returns 200 OK with HTML content
    - And: HTML contains "API Documentation" heading
    - And: HTML includes navigation for endpoint groups

    Test 3 (EndpointCoverage): All API endpoints are documented
    - When: Documentation is generated
    - Then: All routes in routes/api.php appear in documentation
    - And: Each endpoint has HTTP method, path, and description
    - And: Authenticated endpoints show "Authenticated" badge
    - And: Endpoints grouped by @group annotation (Authentication, Tenant Management, Catalog Synchronization, Product Search)

    Test 4 (CurlCommands): Documentation includes curl examples
    - When: Viewing any endpoint documentation
    - Then: Example curl command is displayed
    - And: Command includes correct HTTP method and endpoint URL
    - And: Authenticated endpoints include Authorization header
    - And: Request body examples match FormRequest validation rules

    Test 5 (ResponseSchema): Response schemas are documented
    - When: Viewing any endpoint documentation
    - Then: Response fields are listed with data types
    - And: @responseField annotations describe each field
    - And: Example responses show actual JSON structure
    - And: Error responses (401, 422, 404) are documented
  </behavior>
  <implementation>
    RED Phase (this plan):
    - Create 5 test files with placeholder assertions using assertTrue(true)
    - Each test file covers one requirement area (APIDOCS-01 through APIDOCS-05)
    - Follow Phase 09-00 pattern: "Test organization: 4 feature test files (integration) + 1 unit test file"
    - Use RefreshDatabase trait for integration tests
    - Name test methods descriptively (test_{feature}_{behavior})

    GREEN Phase (Plan 11-01):
    - Implement Scribe installation and configuration
    - Run php artisan scribe:generate
    - Replace assertTrue(true) with actual assertions
    - Verify tests pass

    REFACTOR Phase (if needed):
    - Clean up test organization
    - Extract shared test utilities
    - Improve test readability
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ScribeGenerationTest.php with RED phase placeholders</name>
  <files>tests/Feature/ScribeGenerationTest.php</files>
  <behavior>
    - Test: Scribe generates public/docs directory
    - Test: Documentation includes all API endpoints
    - Test: Generated HTML is valid and accessible
  </behavior>
  <action>
    Create tests/Feature/ScribeGenerationTest.php with RED phase placeholders:
    - Use RefreshDatabase trait
    - Add test_scribe_generates_documentation() with assertTrue(true)
    - Add test_documentation_includes_all_endpoints() with assertTrue(true)
    - Add test_generated_html_is_valid() with assertTrue(true)
    - Follow Phase 09-00 pattern: placeholder assertions for Nyquist compliance
  </action>
  <verify>
    <automated>php artisan test tests/Feature/ScribeGenerationTest.php</automated>
  </verify>
  <done>Test file exists with 3 placeholder assertions, all tests pass (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create DocumentationEndpointTest.php with RED phase placeholders</name>
  <files>tests/Feature/DocumentationEndpointTest.php</files>
  <behavior>
    - Test: GET /docs returns 200 OK
    - Test: Response contains HTML content
    - Test: HTML includes API Documentation heading
  </behavior>
  <action>
    Create tests/Feature/DocumentationEndpointTest.php with RED phase placeholders:
    - Use RefreshDatabase trait
    - Add test_docs_endpoint_returns_200() with assertTrue(true)
    - Add test_docs_returns_html_content() with assertTrue(true)
    - Add test_html_includes_documentation_heading() with assertTrue(true)
    - Comments indicate these test /docs route accessibility (APIDOCS-02)
  </action>
  <verify>
    <automated>php artisan test tests/Feature/DocumentationEndpointTest.php</automated>
  </verify>
  <done>Test file exists with 3 placeholder assertions, all tests pass (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create EndpointCoverageTest.php with RED phase placeholders</name>
  <files>tests/Feature/EndpointCoverageTest.php</files>
  <behavior>
    - Test: All API routes appear in documentation
    - Test: Endpoints grouped by @group annotation
    - Test: Authenticated endpoints show authentication badge
    - Test: Tenant-scoped endpoints document X-Tenant-ID header
  </behavior>
  <action>
    Create tests/Feature/EndpointCoverageTest.php with RED phase placeholders:
    - Use RefreshDatabase trait
    - Add test_all_api_routes_documented() with assertTrue(true)
    - Add test_endpoints_grouped_by_controller() with assertTrue(true)
    - Add test_authenticated_endpoints_show_badge() with assertTrue(true)
    - Add test_tenant_scoped_endpoints_document_header() with assertTrue(true)
    - Comments reference routes/api.php structure (11 controllers, 5 groups)
  </action>
  <verify>
    <automated>php artisan test tests/Feature/EndpointCoverageTest.php</automated>
  </verify>
  <done>Test file exists with 4 placeholder assertions, all tests pass (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create CurlCommandsTest.php with RED phase placeholders</name>
  <files>tests/Feature/CurlCommandsTest.php</files>
  <behavior>
    - Test: Each endpoint shows curl example
    - Test: Commands include correct HTTP method
    - Test: Authenticated commands include Authorization header
    - Test: Request body examples match validation rules
  </behavior>
  <action>
    Create tests/Feature/CurlCommandsTest.php with RED phase placeholders:
    - Use RefreshDatabase trait
    - Add test_endpoints_have_curl_examples() with assertTrue(true)
    - Add test_curl_commands_use_correct_http_method() with assertTrue(true)
    - Add test_authenticated_commands_include_authorization_header() with assertTrue(true)
    - Add test_request_examples_match_validation_rules() with assertTrue(true)
    - Comments reference FormRequest classes (CreateTenantRequest, LoginRequest, etc.)
  </action>
  <verify>
    <automated>php artisan test tests/Feature/CurlCommandsTest.php</automated>
  </verify>
  <done>Test file exists with 4 placeholder assertions, all tests pass (RED phase)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Create ResponseSchemaTest.php with RED phase placeholders</name>
  <files>tests/Feature/ResponseSchemaTest.php</files>
  <behavior>
    - Test: Response fields documented with data types
    - Test: @responseField annotations present
    - Test: Example responses show JSON structure
    - Test: Error responses documented (401, 422, 404)
  </behavior>
  <action>
    Create tests/Feature/ResponseSchemaTest.php with RED phase placeholders:
    - Use RefreshDatabase trait
    - Add test_response_fields_have_data_types() with assertTrue(true)
    - Add test_response_field_annotations_present() with assertTrue(true)
    - Add test_example_responses_show_json_structure() with assertTrue(true)
    - Add test_error_responses_documented() with assertTrue(true)
    - Comments reference API Resource classes (UserResource, TenantResource, SyncLogResource)
  </action>
  <verify>
    <automated>php artisan test tests/Feature/ResponseSchemaTest.php</automated>
  </verify>
  <done>Test file exists with 4 placeholder assertions, all tests pass (RED phase)</done>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] All 5 test files created in tests/Feature/
- [ ] Each test file has 3-4 placeholder assertions
- [ ] All tests pass with assertTrue(true) (RED phase)
- [ ] Test files cover all 5 requirements (APIDOCS-01 through APIDOCS-05)
- [ ] Test structure follows Phase 09-00 TDD pattern
- [ ] Comments indicate what will be tested in GREEN phase
</verification>

<success_criteria>
1. Test suite runs successfully with all placeholder assertions passing
2. 5 test files created covering Scribe generation, documentation endpoint, endpoint coverage, curl commands, and response schemas
3. Total of 18 placeholder assertions (3+3+4+4+4) ready for GREEN phase implementation
4. Test structure documented for implementation in Plan 11-01
5. Nyquist compliance satisfied: all tests have assertions (even if placeholders)
</success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-api-documentation/11-00-SUMMARY.md`
</output>
