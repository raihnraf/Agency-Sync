---
phase: 11-interactive-api-documentation
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/Http/Controllers/Api/V1/AuthController.php
  - app/Http/Controllers/Api/V1/TenantController.php
  - app/Http/Controllers/Api/V1/SyncController.php
  - app/Http/Controllers/Api/V1/ProductSearchController.php
  - app/Http/Controllers/Api/V1/IndexController.php
autonomous: true
requirements:
  - APIDOCS-03
  - APIDOCS-04
  - APIDOCS-05

must_haves:
  truths:
    - "All API controllers have @group annotations for logical grouping"
    - "All authenticated endpoints have @authenticated annotations"
    - "All endpoints have descriptive docblocks with parameter documentation"
    - "Request parameters documented with @bodyParam or @urlParam annotations"
    - "Response fields documented with @responseField annotations"
    - "Example responses provided with @response annotations"
    - "Error responses documented (401, 422, 404)"
  artifacts:
    - path: "app/Http/Controllers/Api/V1/AuthController.php"
      provides: "Authentication endpoint documentation"
      contains: "@group Authentication"
    - path: "app/Http/Controllers/Api/V1/TenantController.php"
      provides: "Tenant management endpoint documentation"
      contains: "@group Tenant Management"
    - path: "app/Http/Controllers/Api/V1/SyncController.php"
      provides: "Catalog sync endpoint documentation"
      contains: "@group Catalog Synchronization"
    - path: "app/Http/Controllers/Api/V1/ProductSearchController.php"
      provides: "Product search endpoint documentation"
      contains: "@group Product Search"
    - path: "app/Http/Controllers/Api/V1/IndexController.php"
      provides: "Index management endpoint documentation"
      contains: "@group Index Management"
  key_links:
    - from: "app/Http/Controllers/Api/V1/*.php"
      to: "config/scribe.php"
      via: "@group annotations"
      pattern: "Scribe reads @group for navigation"
    - from: "app/Http/Controllers/Api/V1/*Controller.php"
      to: "app/Http/Requests/*Request.php"
      via: "@bodyParam annotations"
      pattern: "FormRequest rules auto-documented"
    - from: "app/Http/Controllers/Api/V1/*Controller.php"
      to: "app/Http/Resources/*Resource.php"
      via: "@responseField annotations"
      pattern: "API Resource structure documented"
---

<objective>
Add comprehensive PHPDoc annotations to all API controllers for automatic documentation generation.

Purpose: Enable Scribe to generate detailed, beautiful API documentation by adding structured annotations to controllers. This demonstrates API-first backend development by making all endpoints self-documenting with examples, schemas, and authentication requirements.

Output: Fully documented API controllers with @group, @authenticated, @bodyParam, @responseField, and @response annotations.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-interactive-api-documentation/11-RESEARCH.md
@.planning/phases/11-interactive-api-documentation/11-01-SUMMARY.md
@.planning/phases/11-interactive-api-documentation/11-00-SUMMARY.md

# Existing Controller Structure (from routes/api.php)
- AuthController: register, login, logout, me (4 endpoints)
- TenantController: index, store, show, update, destroy (5 endpoints)
- SyncController: dispatch, status, history (3 endpoints)
- ProductSearchController: search, status, reindex (3 endpoints)
- IndexController: reindex, status, list (3 endpoints)

# Current Docblock State (from AuthController.php, TenantController.php read earlier)
- AuthController has minimal docblocks: "Register a new user.", "Login user and return token."
- TenantController has minimal docblocks: "Display a listing of the user's tenants."
- Missing: @group, @authenticated, @bodyParam, @responseField, @response annotations

# 11-RESEARCH.md Annotation Pattern
```php
/**
 * @group Authentication
 *
 * API endpoints for user authentication and token management
 */
class AuthController extends BaseController
{
    /**
     * Register a new user.
     *
     * Creates a new user account and returns an authentication token.
     *
     * @bodyParam name string required The user's full name. Example: John Doe
     * @bodyParam email string required The user's email address. Example: john@example.com
     * @bodyParam password string required The user's password (min 8 characters). Example: secret123
     *
     * @response {
     *   "data": {
     *     "user": {...},
     *     "token": "plainTextTokenHere"
     *   },
     *   "meta": {
     *     "expires_at": "2026-03-15T12:00:00Z"
     *   }
     * }
     */
    public function register(RegisterRequest $request)
    {
        // ...
    }
}
```

# Existing FormRequest Classes (for reference)
- RegisterRequest, LoginRequest (Auth)
- CreateTenantRequest, UpdateTenantRequest (Tenant)
- Validation rules already defined, Scribe will auto-document from these

# Existing API Resource Classes (for reference)
- UserResource, TenantResource, SyncLogResource, ProductResource
- Response structure already defined, use as basis for @responseField annotations
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document AuthController with comprehensive annotations</name>
  <files>app/Http/Controllers/Api/V1/AuthController.php</files>
  <action>
    Add comprehensive PHPDoc annotations to AuthController:

    Class-level docblock:
    ```php
    /**
     * @group Authentication
     *
     * API endpoints for user authentication and token management
     */
    ```

    Method annotations:
    1. register(): Public endpoint, no @authenticated
       - @bodyParam for name, email, password (all required)
       - @response showing success (201) with user and token
       - @response 422 for validation errors

    2. login(): Public endpoint, no @authenticated
       - @bodyParam for email, password (all required)
       - @response showing success (200) with user and token
       - @response 401 for invalid credentials

    3. logout(): Authenticated endpoint
       - @authenticated annotation
       - @response 204 (no content)
       - Note: Invalidates current token

    4. me(): Authenticated endpoint
       - @authenticated annotation
       - @responseField data.user.id string User UUID
       - @responseField data.user.name string User's full name
       - @responseField data.user.email string User's email address
       - @response showing user data

    Follow 11-RESEARCH.md examples for exact annotation syntax.
  </action>
  <verify>
    <automated>php artisan scribe:generate && grep -q "Authentication" public/docs/index.html && echo "Auth docs generated"</automated>
  </verify>
  <done>AuthController has @group annotation, all methods have @bodyParam/@response/@responseField, authenticated methods marked</done>
</task>

<task type="auto">
  <name>Task 2: Document TenantController with comprehensive annotations</name>
  <files>app/Http/Controllers/Api/V1/TenantController.php</files>
  <action>
    Add comprehensive PHPDoc annotations to TenantController:

    Class-level docblock:
    ```php
    /**
     * @group Tenant Management
     *
     * API endpoints for managing client stores (tenants)
     */
    ```

    Method annotations:
    1. index(): Authenticated, no tenant context required
       - @authenticated annotation
       - @responseField data{0}.id string Tenant UUID
       - @responseField data{0}.name string Tenant name
       - @responseField data{0}.slug string URL-friendly identifier
       - @responseField data{0}.status string Tenant status (active, pending, error)
       - @responseField data{0}.platform_type string Platform (shopify, shopware)
       - @response showing array of tenants

    2. store(): Authenticated, creates new tenant
       - @authenticated annotation
       - @bodyParam name string required Tenant name
       - @bodyParam platform_type string required Platform (shopify, shopware)
       - @bodyParam platform_url string required Platform base URL
       - @bodyParam api_credentials object required API credentials
       - @bodyParam api_credentials.api_key string required API key
       - @bodyParam api_credentials.api_secret string required API secret
       - @response 201 showing created tenant
       - @response 422 for invalid credentials

    3. show(): Authenticated + tenant context
       - @authenticated annotation
       - @urlParam id string required Tenant UUID
       - @header X-Tenant-ID required for tenant-scoped requests
       - @responseField data.id string Tenant UUID
       - @responseField data.name string Tenant name
       - @response showing tenant details

    4. update(): Authenticated + tenant context
       - @authenticated annotation
       - @urlParam id string required Tenant UUID
       - @header X-Tenant-ID required
       - @bodyParam name string optional Updated tenant name
       - @bodyParam status string optional Updated status
       - @response 200 showing updated tenant
       - @response 422 for validation errors

    5. destroy(): Authenticated + tenant context
       - @authenticated annotation
       - @urlParam id string required Tenant UUID
       - @header X-Tenant-ID required
       - @response 204 (no content)
       - @response 404 if tenant not found

    Reference: CreateTenantRequest and UpdateTenantRequest validation rules for body params.
  </action>
  <verify>
    <automated>php artisan scribe:generate && grep -q "Tenant Management" public/docs/index.html && echo "Tenant docs generated"</automated>
  </verify>
  <done>TenantController has @group annotation, all methods documented with @bodyParam/@urlParam/@responseField</done>
</task>

<task type="auto">
  <name>Task 3: Document SyncController with comprehensive annotations</name>
  <files>app/Http/Controllers/Api/V1/SyncController.php</files>
  <action>
    Add comprehensive PHPDoc annotations to SyncController:

    Class-level docblock:
    ```php
    /**
     * @group Catalog Synchronization
     *
     * API endpoints for triggering and monitoring product catalog synchronization
     */
    ```

    Method annotations:
    1. dispatch(): Authenticated, triggers async sync job
       - @authenticated annotation
       - @bodyParam tenant_id string required Tenant UUID to sync
       - @bodyParam sync_type string required Sync type (full, incremental)
       - @response 202 with job_id and status (accepted, not completed)
       - @response 422 if tenant not found or invalid

    2. status(): Authenticated, poll sync job status
       - @authenticated annotation
       - @urlParam syncLogId string required Sync log UUID
       - @responseField data.job_id string Job UUID
       - @responseField data.status string Job status (pending, running, completed, failed)
       - @responseField data.progress integer Progress percentage (0-100)
       - @responseField data.products_indexed integer Number of products processed
       - @responseField data.started_at timestamp Job start time
       - @responseField data.completed_at timestamp Job completion time (null if running)
       - @responseField data.error_message string Error message (null if success)
       - @response showing job status

    3. history(): Authenticated + tenant context, get sync history
       - @authenticated annotation
       - @urlParam tenant_id string required Tenant UUID
       - @header X-Tenant-ID required
       - @queryParam page integer Page number (default: 1)
       - @queryParam per_page integer Items per page (default: 20, max: 100)
       - @responseField data{0}.id string Sync log UUID
       - @responseField data{0}.status string Sync status
       - @responseField data{0}.products_indexed integer Products indexed
       - @responseField data{0}.started_at timestamp Start time
       - @responseField data{0}.completed_at timestamp Completion time
       - @response showing paginated sync history

    Reference: SyncLogResource for response field structure.
  </action>
  <verify>
    <automated>php artisan scribe:generate && grep -q "Catalog Synchronization" public/docs/index.html && echo "Sync docs generated"</automated>
  </verify>
  <done>SyncController has @group annotation, all methods documented with async job status fields</done>
</task>

<task type="auto">
  <name>Task 4: Document ProductSearchController with comprehensive annotations</name>
  <files>app/Http/Controllers/Api/V1/ProductSearchController.php</files>
  <action>
    Add comprehensive PHPDoc annotations to ProductSearchController:

    Class-level docblock:
    ```php
    /**
     * @group Product Search
     *
     * API endpoints for searching products within tenant catalogs using Elasticsearch
     */
    ```

    Method annotations:
    1. search(): Authenticated, search products
       - @authenticated annotation
       - @urlParam tenantId string required Tenant UUID
       - @queryParam query string required Search query
       - @queryParam page integer Page number (default: 1)
       - @queryParam per_page integer Results per page (default: 20, max: 100)
       - @queryParam filters string optional JSON filters (stock_status, price_range)
       - @responseField data{0}.id string Product UUID
       - @responseField data{0}.name string Product name
       - @responseField data{0}.price number Product price
       - @responseField data{0}.stock_status string Stock status (in_stock, low_stock, out_of_stock)
       - @responseField meta.pagination.total integer Total results
       - @responseField meta.pagination.per_page integer Results per page
       - @responseField meta.pagination.current_page integer Current page
       - @response showing search results with pagination

    2. status(): Authenticated, get search index status
       - @authenticated annotation
       - @urlParam tenantId string required Tenant UUID
       - @responseField data.index_name string Elasticsearch index name
       - @responseField data.documents_count integer Number of indexed products
       - @responseField data.last_updated timestamp Last index update time
       - @response showing index status

    3. reindex(): Authenticated, trigger reindex job
       - @authenticated annotation
       - @urlParam tenantId string required Tenant UUID
       - @bodyParam force boolean optional Force full reindex (default: false)
       - @response 202 with job_id and status (accepted)
       - @response 422 if tenant not found

    Reference: ProductResource for response field structure.
  </action>
  <verify>
    <automated>php artisan scribe:generate && grep -q "Product Search" public/docs/index.html && echo "Search docs generated"</automated>
  </verify>
  <done>ProductSearchController has @group annotation, search parameters and pagination documented</done>
</task>

<task type="auto">
  <name>Task 5: Document IndexController with comprehensive annotations</name>
  <files>app/Http/Controllers/Api/V1/IndexController.php</files>
  <action>
    Add comprehensive PHPDoc annotations to IndexController:

    Class-level docblock:
    ```php
    /**
     * @group Index Management
     *
     * API endpoints for managing Elasticsearch index operations (async)
     */
    ```

    Method annotations:
    1. reindex(): Authenticated, trigger Elasticsearch reindex
       - @authenticated annotation
       - @urlParam tenantId string required Tenant UUID
       - @response 202 with job_id and status
       - @responseField data.job_id string Reindex job UUID
       - @responseField data.status string Job status (pending, running)
       - @response 422 if tenant not found

    2. status(): Authenticated, get index job status
       - @authenticated annotation
       - @urlParam jobId string required Job UUID
       - @responseField data.job_id string Job UUID
       - @responseField data.status string Job status
       - @responseField data.progress integer Progress percentage
       - @responseField data.started_at timestamp Start time
       - @responseField data.completed_at timestamp Completion time
       - @response showing job status

    3. list(): Authenticated, list all index jobs for tenant
       - @authenticated annotation
       - @urlParam tenantId string required Tenant UUID
       - @queryParam status string optional Filter by status (pending, running, completed, failed)
       - @queryParam page integer Page number (default: 1)
       - @responseField data{0}.job_id string Job UUID
       - @responseField data{0}.status string Job status
       - @responseField data{0}.created_at timestamp Job creation time
       - @response showing paginated job list

    Reference: JobStatus model for response field structure.
  </action>
  <verify>
    <automated>php artisan scribe:generate && grep -q "Index Management" public/docs/index.html && echo "Index docs generated"</automated>
  </verify>
  <done>IndexController has @group annotation, async job operations documented with status fields</done>
</task>

</tasks>

<verification>
Overall phase verification:
- [ ] All 5 controllers have @group class-level annotations
- [ ] All authenticated endpoints have @authenticated annotation
- [ ] All endpoints have descriptive method docblocks
- [ ] Request parameters documented with @bodyParam or @urlParam
- [ ] Response fields documented with @responseField
- [ ] Example responses provided with @response annotations
- [ ] Error responses documented (401, 422, 404)
- [ ] php artisan scribe:generate completes successfully
- [ ] Generated documentation shows all annotations in HTML
</verification>

<success_criteria>
1. All 18 API endpoints (4+5+3+3+3) fully documented with annotations
2. Documentation organized into 5 logical groups (Authentication, Tenant Management, Catalog Synchronization, Product Search, Index Management)
3. Authenticated endpoints clearly marked with "Authenticated" badge in docs
4. Request parameters documented with types and examples
5. Response schemas documented with field types and descriptions
6. Example responses show actual JSON structure
7. Ready for Plan 11-03: Regenerate documentation and integrate with deployment
</success_criteria>

<output>
After completion, create `.planning/phases/11-interactive-api-documentation/11-02-SUMMARY.md`
</output>
