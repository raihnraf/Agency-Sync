---
phase: 14-critical-frontend-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/dashboard.js
autonomous: true
requirements:
  - SYNC-01
  - UI-05
gap_closure: true

must_haves:
  truths:
    - "Sync trigger button calls correct /sync/dispatch endpoint without runtime errors"
    - "Request includes tenant_id in body (not URL parameter)"
    - "Users can trigger manual catalog sync for client stores"
  artifacts:
    - path: "public/js/dashboard.js"
      provides: "Sync trigger functionality with correct variable references"
      min_lines: 1
      contains: "this.tenantId"
  key_links:
    - from: "public/js/dashboard.js:189"
      to: "/api/v1/sync/dispatch"
      via: "fetch with request body"
      pattern: "tenant_id: this.tenantId"
---

<objective>
Fix undefined variable bug in dashboard.js line 189 that breaks sync trigger functionality, closing SYNC-01 and UI-05 requirements blocked by ReferenceError.

Purpose: Verification report found critical bug where `tenantId` (undefined) is used instead of `this.tenantId`, causing runtime failure when users click sync trigger button. This is a simple variable reference fix with high impact.

Output: Working sync trigger button that successfully calls /api/v1/sync/dispatch endpoint with proper tenant_id value.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-critical-frontend-fixes/14-VERIFICATION.md
@.planning/phases/14-critical-frontend-fixes/14-02-SUMMARY.md

# Gap from VERIFICATION.md

**Gap Source:** Line 189 in public/js/dashboard.js
**Issue:** Uses undefined variable `tenantId` instead of `this.tenantId`
**Impact:** Runtime ReferenceError breaks sync trigger completely
**Requirements Blocked:** SYNC-01, UI-05
**Fix Required:** Change `tenant_id: tenantId` to `tenant_id: this.tenantId`

# What Works (from 14-02-SUMMARY.md)

From plan 14-02, the sync dispatch endpoint was corrected:
- Endpoint URL changed to `/api/v1/sync/dispatch` ✓
- Request body structure added with `tenant_id` and `data` fields ✓
- Line 181 was fixed correctly ✓

**What was missed:** Line 189 has the same bug but was not fixed
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix undefined variable bug in sync trigger function (line 189)</name>
  <files>public/js/dashboard.js</files>
  <behavior>
    - Test 1: Sync trigger button creates fetch request with valid tenant_id value
    - Test 2: Request body contains tenant_id from this.tenantId (not undefined tenantId)
    - Test 3: No JavaScript ReferenceError thrown when sync button clicked
  </behavior>
  <action>
    Fix line 189 in public/js/dashboard.js:
    - Change `tenant_id: tenantId` to `tenant_id: this.tenantId`
    - This is within the triggerSync() function's fetch() call
    - The variable reference bug causes tenant_id value to be undefined at runtime
    - Fix ensures this.tenantId (which is defined in the Alpine.js component) is used

    Reference from 14-02-SUMMARY.md: Line 181 was fixed correctly with this.tenantId, but line 189 was missed
    Verification report line 24: "Line 189 uses undefined 'tenantId' instead of 'this.tenantId'"

    Do NOT modify any other parts of the file - this is a single-character fix (add "this.")
  </action>
  <verify>
    <automated>php artisan test --filter=SyncTriggerUIIntegrationTest</automated>
  </verify>
  <done>Line 189 now reads `tenant_id: this.tenantId`, sync trigger button works without ReferenceError, SYNC-01 and UI-05 requirements satisfied</done>
</task>

</tasks>

<verification>
After fix, verify:
1. Line 189 reads `tenant_id: this.tenantId` (not `tenantId`)
2. All SyncTriggerUIIntegrationTest tests pass
3. Manual test: Load dashboard, click sync trigger button, no console errors
</verification>

<success_criteria>
- [ ] public/js/dashboard.js line 189 fixed: `tenant_id: this.tenantId`
- [ ] No undefined variable references in sync trigger code
- [ ] SyncTriggerUIIntegrationTest passes
- [ ] Manual verification: sync button works without JavaScript errors
- [ ] SYNC-01 requirement satisfied: Agency admin can trigger manual catalog sync
- [ ] UI-05 requirement satisfied: Agency admin can trigger sync for each client store
</success_criteria>

<output>
After completion, create `.planning/phases/14-critical-frontend-fixes/14-03-SUMMARY.md`
</output>
