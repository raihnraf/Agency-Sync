---
phase: 14-critical-frontend-fixes
plan: 05
type: execute
wave: 2
depends_on:
  - 14-03
  - 14-04
files_modified:
  - tests/Feature/ProductSearchEndpointTest.php
  - tests/Feature/ProductSearchUIIntegrationTest.php
  - tests/Feature/SyncTriggerUIIntegrationTest.php
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-07
  - SYNC-01
  - UI-05
  - UI-07
gap_closure: true

must_haves:
  truths:
    - "All test files have real assertions (GREEN phase)"
    - "Tests verify actual frontend-backend integration behavior"
    - "No placeholder assertTrue(true) assertions remain"
    - "Tests explicitly verify bug fixes from 14-03 (line 189) and 14-04 (line 809)"
  artifacts:
    - path: "tests/Feature/ProductSearchEndpointTest.php"
      provides: "API endpoint tests for product search"
      min_lines: 48
      exports: ["test_product_search_endpoint_returns_200_for_authenticated_user", "test_product_search_endpoint_requires_authentication", "test_product_search_results_scoped_to_tenant", "test_product_search_accepts_query_and_page_parameters"]
    - path: "tests/Feature/ProductSearchUIIntegrationTest.php"
      provides: "Frontend integration tests for product search"
      min_lines: 48
      exports: ["test_dashboard_search_calls_correct_search_endpoint", "test_dashboard_search_includes_tenant_id_in_request", "test_dashboard_search_handles_api_errors", "test_dashboard_search_updates_ui_with_results"]
    - path: "tests/Feature/SyncTriggerUIIntegrationTest.php"
      provides: "Frontend integration tests for sync trigger"
      min_lines: 48
      exports: ["test_dashboard_sync_button_calls_dispatch_endpoint", "test_dashboard_sync_includes_tenant_id_in_request_body", "test_dashboard_sync_handles_202_response", "test_dashboard_sync_disables_button_during_sync"]
  key_links:
    - from: "tests/Feature/ProductSearchEndpointTest.php"
      to: "/api/v1/tenants/{tenantId}/search"
      via: "HTTP test assertions"
      pattern: "assertOk|assertJson|assertStatus"
    - from: "tests/Feature/SyncTriggerUIIntegrationTest.php"
      to: "/api/v1/sync/dispatch"
      via: "HTTP test assertions"
      pattern: "assertStatus\(202\)"
    - from: "tests/Feature/SyncTriggerUIIntegrationTest.php"
      to: "public/js/dashboard.js:189"
      via: "Bug fix verification"
      pattern: "this\.tenantId.*tenant_id"
---

<objective>
Implement real assertions in 3 placeholder test files (ProductSearchEndpointTest, ProductSearchUIIntegrationTest, SyncTriggerUIIntegrationTest) to move from RED phase to GREEN phase, closing verification gap about false test security.

Purpose: Verification report found 3 of 4 test files still have assertTrue(true) placeholder assertions, providing false sense of security. These tests need real assertions that verify actual frontend-backend integration behavior. SyncDispatchEndpointTest already has real assertions (reference implementation).

Output: Test files with real assertions that verify search endpoint authentication, tenant scoping, parameter handling, and frontend API call behavior.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-critical-frontend-fixes/14-VERIFICATION.md
@.planning/phases/14-critical-frontend-fixes/14-01-SUMMARY.md
@.planning/phases/14-critical-frontend-fixes/14-02-SUMMARY.md
@.planning/phases/14-critical-frontend-fixes/14-03-PLAN.md
@.planning/phases/14-critical-frontend-fixes/14-04-PLAN.md

# Gap from VERIFICATION.md

**Gap Source:** 3 test files with placeholder assertions
**Issue:** All tests use assertTrue(true) placeholders - false sense of security
**Impact:** Tests pass but don't verify actual behavior
**Affected Files:**
- ProductSearchEndpointTest.php (4 placeholder tests)
- ProductSearchUIIntegrationTest.php (4 placeholder tests)
- SyncTriggerUIIntegrationTest.php (4 placeholder tests)

# Reference Implementation

SyncDispatchEndpointTest (from 14-02-SUMMARY.md) has real assertions:
```php
public function test_sync_dispatch_returns_202_accepted(): void
{
    $user = User::factory()->create();
    $tenant = Tenant::factory()->hasAttached($user)->create();

    Sanctum::actingAs($user);
    Queue::fake();

    $response = $this->postJson('/api/v1/sync/dispatch', [
        'tenant_id' => $tenant->id,
        'data' => []
    ]);

    $response->assertStatus(202);
    $response->assertJsonStructure(['data' => ['job_uuid', 'status']]);
}
```

# What Was Already Fixed

From 14-01-SUMMARY.md: Product search frontend was corrected to call /search endpoint
From 14-02-SUMMARY.md: Sync trigger frontend was corrected to call /sync/dispatch endpoint
From 14-03-PLAN.md: Line 189 undefined variable bug fixed (depends_on this plan)
From 14-04-PLAN.md: Line 809 undefined variable bug fixed (depends_on this plan)

These tests need to verify that the frontend fixes are actually working.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement real assertions in ProductSearchEndpointTest (GREEN phase)</name>
  <files>tests/Feature/ProductSearchEndpointTest.php</files>
  <behavior>
    - Test 1: Authenticated user can access product search endpoint (200 status)
    - Test 2: Unauthenticated request returns 401 Unauthorized
    - Test 3: Search results only include products from requested tenant (tenant isolation)
    - Test 4: Endpoint accepts query and page parameters correctly
  </behavior>
  <action>
    Replace all assertTrue(true) placeholders with real assertions in ProductSearchEndpointTest.php:

    Test 1: test_product_search_endpoint_returns_200_for_authenticated_user
    - Create authenticated user with tenant relationship
    - Create products in tenant's catalog
    - Call GET /api/v1/tenants/{tenantId}/search with query
    - Assert response status is 200
    - Assert response has JSON structure with data and meta fields
    - Reference SyncDispatchEndpointTest pattern for authentication setup

    Test 2: test_product_search_endpoint_requires_authentication
    - Create tenant but don't authenticate user
    - Call GET /api/v1/tenants/{tenantId}/search
    - Assert response status is 401
    - Reference SyncDispatchEndpointTest auth pattern

    Test 3: test_product_search_results_scoped_to_tenant
    - Create user with tenant A
    - Create tenant B (not associated with user)
    - Create products in both tenant A and tenant B
    - Authenticate as user with tenant A
    - Call search endpoint for tenant A
    - Assert results only contain products from tenant A
    - Assert no products from tenant B in results (tenant isolation)

    Test 4: test_product_search_accepts_query_and_page_parameters
    - Create authenticated user with tenant
    - Create 30 products in tenant's catalog
    - Call search with query parameter (e.g., ?query=test)
    - Call search with page parameter (e.g., ?page=2)
    - Assert response includes only matching products for query
    - Assert response includes correct pagination for page parameter
    - Reference 14-01-SUMMARY.md: endpoint uses /search not /products

    Use RefreshDatabase trait (already present)
    Use Sanctum::actingAs() for authentication
    Follow Laravel 11 testing patterns from SyncDispatchEndpointTest
  </action>
  <verify>
    <automated>grep -c "assertStatus\|assertOk\|assertJson\|assertTrue\|assertCount" tests/Feature/ProductSearchEndpointTest.php | awk '{if ($1 > 4) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>All 4 tests in ProductSearchEndpointTest have real assertions and pass, verifying search endpoint authentication, tenant scoping, and parameter handling (SEARCH-01, SEARCH-07 requirements satisfied)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement real assertions in ProductSearchUIIntegrationTest (GREEN phase)</name>
  <files>tests/Feature/ProductSearchUIIntegrationTest.php</files>
  <behavior>
    - Test 1: Dashboard search JavaScript calls correct /search endpoint
    - Test 2: Dashboard search includes tenant_id in API request URL
    - Test 3: Dashboard search handles API errors gracefully
    - Test 4: Dashboard search updates UI with search results
  </behavior>
  <action>
    Replace all assertTrue(true) placeholders with real assertions in ProductSearchUIIntegrationTest.php:

    Test 1: test_dashboard_search_calls_correct_search_endpoint
    - This is a frontend integration test - need to verify JavaScript makes correct API call
    - Use Laravel's HTTP testing to mock the expected endpoint
    - Create authenticated user with tenant
    - Call frontend endpoint or simulate the fetch call
    - Assert that GET /api/v1/tenants/{tenantId}/search is called
    - Reference 14-01-SUMMARY.md: dashboard.js line 471 calls /search endpoint
    - Note: This may require Browser testing (Laravel Dusk) or HTTP assertion

    Test 2: test_dashboard_search_includes_tenant_id_in_request
    - Verify tenant_id is included in search request URL
    - Create authenticated user with tenant
    - Simulate search API call
    - Assert URL contains correct tenantId parameter
    - Reference 14-01-SUMMARY.md: endpoint uses /api/v1/tenants/${this.tenantId}/search

    Test 3: test_dashboard_search_handles_api_errors
    - Simulate API error response (e.g., 500 or 404)
    - Verify frontend handles error gracefully
    - Assert error message displayed to user
    - Assert no console errors thrown

    Test 4: test_dashboard_search_updates_ui_with_results
    - Create products in tenant's catalog
    - Simulate search API call returning results
    - Verify UI updates with search results
    - Assert product list is populated
    - Reference 14-01-SUMMARY.md: product search integration working

    Note: Frontend integration tests may be limited without browser automation
    Focus on HTTP-level integration that can be tested with PHPUnit
    If full UI testing needed, note that Laravel Dusk would be required
  </action>
  <verify>
    <automated>grep -c "assertStatus\|assertOk\|assertJson\|assertTrue\|assertCount" tests/Feature/ProductSearchUIIntegrationTest.php | awk '{if ($1 > 4) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>All 4 tests in ProductSearchUIIntegrationTest have real assertions and pass, verifying dashboard search integration (UI-07 requirement satisfied)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement real assertions in SyncTriggerUIIntegrationTest (GREEN phase)</name>
  <files>tests/Feature/SyncTriggerUIIntegrationTest.php</files>
  <behavior>
    - Test 1: Dashboard sync button calls /api/v1/sync/dispatch endpoint
    - Test 2: Dashboard sync includes tenant_id in request body
    - Test 3: Dashboard sync handles 202 Accepted response
    - Test 4: Dashboard sync disables button during sync
  </behavior>
  <action>
    Replace all assertTrue(true) placeholders with real assertions in SyncTriggerUIIntegrationTest.php:

    Test 1: test_dashboard_sync_button_calls_dispatch_endpoint
    - Create authenticated user with tenant
    - Simulate frontend sync trigger POST request
    - Assert POST /api/v1/sync/dispatch is called
    - Reference 14-02-SUMMARY.md: endpoint changed from /tenants/{id}/sync to /sync/dispatch
    - Reference 14-03 gap closure: line 189 now uses this.tenantId

    Test 2: test_dashboard_sync_includes_tenant_id_in_request_body
    - Create authenticated user with tenant
    - Simulate sync dispatch request
    - Assert request body contains tenant_id field
    - Assert tenant_id value matches user's tenant ID
    - Reference 14-02-SUMMARY.md: body structure is {tenant_id, data}
    - Reference 14-03 gap closure: tenant_id is now this.tenantId (not undefined)
    - CRITICAL: This test must verify that the bug fix from 14-03 is working correctly
    - The test should verify that tenant_id in request body matches the user's actual tenant ID
    - This ensures the undefined variable bug is fixed

    Test 3: test_dashboard_sync_handles_202_response
    - Create authenticated user with tenant
    - Queue::fake() to prevent actual sync job execution
    - Call sync dispatch endpoint
    - Assert response status is 202 Accepted
    - Assert response JSON contains job tracking data
    - Reference SyncDispatchEndpointTest pattern for assertions

    Test 4: test_dashboard_sync_disables_button_during_sync
    - This tests UI state management
    - May require browser automation or frontend state verification
    - Alternative: Verify that sync status polling starts after successful dispatch
    - Assert job status is created in pending state
    - Reference 14-02-SUMMARY.md: sync button disabled during sync

    Use RefreshDatabase trait (already present)
    Use Sanctum::actingAs() for authentication
    Use Queue::fake() to prevent actual job execution
    Follow patterns from SyncDispatchEndpointTest (reference implementation)
  </action>
  <verify>
    <automated>grep -c "assertStatus\|assertOk\|assertJson\|assertTrue\|assertCount" tests/Feature/SyncTriggerUIIntegrationTest.php | awk '{if ($1 > 4) print "PASS"; else print "FAIL"}'</automated>
  </verify>
  <done>All 4 tests in SyncTriggerUIIntegrationTest have real assertions and pass, verifying sync trigger integration including verification that line 189 bug fix is working (SYNC-01, UI-05 requirements satisfied)</done>
</task>

</tasks>

<verification>
After implementation, verify:
1. All 12 tests across 3 files have real assertions (no assertTrue(true) placeholders)
2. All tests pass (100% success rate)
3. Tests verify actual frontend-backend integration behavior
4. Reference SyncDispatchEndpointTest as passing implementation
5. SyncTriggerUIIntegrationTest explicitly verifies that tenant_id in request body is correct (verifies 14-03 bug fix)
6. Run full test suite: php artisan test
</verification>

<success_criteria>
- [ ] ProductSearchEndpointTest: 4 tests with real assertions, all passing
- [ ] ProductSearchUIIntegrationTest: 4 tests with real assertions, all passing
- [ ] SyncTriggerUIIntegrationTest: 4 tests with real assertions, all passing
- [ ] No placeholder assertTrue(true) assertions remain
- [ ] Total test assertions increased from 12 to ~40+ (real assertions)
- [ ] SyncTriggerUIIntegrationTest verifies tenant_id value in request body (verifies 14-03 bug fix)
- [ ] All requirements covered: SEARCH-01, SEARCH-07, SYNC-01, UI-05, UI-07
- [ ] Tests provide actual security (not false sense of security)
</success_criteria>

<output>
After completion, create `.planning/phases/14-critical-frontend-fixes/14-05-SUMMARY.md`
</output>
