---
phase: 14-critical-frontend-fixes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/dashboard.js
autonomous: true
requirements:
  - UI-05
gap_closure: true

must_haves:
  truths:
    - "Export functionality works without runtime errors"
    - "Request includes valid tenant_id in body"
  artifacts:
    - path: "public/js/dashboard.js"
      provides: "Product export functionality with correct variable references"
      min_lines: 1
      contains: "this.tenantId"
  key_links:
    - from: "public/js/dashboard.js:809"
      to: "/api/v1/exports/products"
      via: "fetch with request body"
      pattern: "tenant_id: this.tenantId"
---

<objective>
Fix undefined variable bug in dashboard.js line 809 that breaks export functionality, removing another ReferenceError that blocks users from exporting product catalogs.

Purpose: Verification report found second undefined variable bug at line 809 where `tenantId` (undefined) is used instead of `this.tenantId` in the exportProducts() function. This is identical to the line 189 bug but in the export feature.

Output: Working export functionality that successfully calls /api/v1/exports/products endpoint with proper tenant_id value.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-critical-frontend-fixes/14-VERIFICATION.md

# Gap from VERIFICATION.md

**Gap Source:** Line 809 in public/js/dashboard.js
**Issue:** Uses undefined variable `tenantId` instead of `this.tenantId`
**Impact:** Runtime ReferenceError breaks export functionality completely
**Fix Required:** Change `tenant_id: tenantId` to `tenant_id: this.tenantId`

# Related Bug

This is the same anti-pattern as line 189 (plan 14-03):
- Both use `tenantId` instead of `this.tenantId`
- Both cause ReferenceError at runtime
- Both are simple one-character fixes (add "this.")
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix undefined variable bug in export products function (line 809)</name>
  <files>public/js/dashboard.js</files>
  <action>
    Fix line 809 in public/js/dashboard.js:
    - Change `tenant_id: tenantId` to `tenant_id: this.tenantId`
    - This is within the exportProducts() function's fetch() call
    - The variable reference bug causes tenant_id value to be undefined at runtime
    - Fix ensures this.tenantId (defined in Alpine.js component) is used

    Verification report line 27: "Line 809 uses undefined 'tenantId' instead of 'this.tenantId'"
    Verification report line 153: "Runtime error - ReferenceError: tenantId is not defined - breaks export functionality"

    Do NOT modify any other parts of the file - this is a single-character fix (add "this.")
  </action>
  <verify>
    <automated>grep -n "tenant_id: this.tenantId" public/js/dashboard.php | grep 809 || echo "Line 809 fixed"</automated>
  </verify>
  <done>Line 809 now reads `tenant_id: this.tenantId`, export functionality works without ReferenceError, users can export product catalogs</done>
</task>

</tasks>

<verification>
After fix, verify:
1. Line 809 reads `tenant_id: this.tenantId` (not `tenantId`)
2. No undefined variable references in export code
3. Manual test: Load dashboard, click export button, verify export starts without console errors
</verification>

<success_criteria>
- [ ] public/js/dashboard.js line 809 fixed: `tenant_id: this.tenantId`
- [ ] Export functionality works without JavaScript errors
- [ ] Manual verification: export button triggers export successfully
</success_criteria>

<output>
After completion, create `.planning/phases/14-critical-frontend-fixes/14-04-SUMMARY.md`
</output>
