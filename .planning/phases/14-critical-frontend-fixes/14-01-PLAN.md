---
phase: 14-critical-frontend-fixes
plan: 01
type: execute
wave: 1
depends_on:
  - 14-00
files_modified:
  - public/js/dashboard.js
  - resources/js/components/product-search.js
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-07
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Product search UI calls correct /search endpoint (not /products)"
    - "Users can search products within client catalogs"
    - "Search results only include products from selected client store"
    - "Frontend-backend integration working end-to-end"
  artifacts:
    - path: "public/js/dashboard.js"
      provides: "Main dashboard product search functionality"
      contains: "fetch call to /api/v1/tenants/{tenantId}/search"
    - path: "resources/js/components/product-search.js"
      provides: "Reusable product search component"
      contains: "fetch call to /api/v1/tenants/{tenantId}/search"
  key_links:
    - from: "public/js/dashboard.js:467"
      to: "routes/api.php:78"
      via: "GET /api/v1/tenants/{tenantId}/search"
      pattern: "fetch.*tenants/.*search"
    - from: "public/js/dashboard.js"
      to: "app/Http/Controllers/Api/V1/ProductSearchController.php"
      via: "Product search API endpoint"
      pattern: "ProductSearchController@search"
    - from: "resources/js/components/product-search.js:33"
      to: "routes/api.php:78"
      via: "GET /api/v1/tenants/{tenantId}/search"
      pattern: "fetch.*tenants/.*search"
---

<objective>
Fix product search frontend to call correct API endpoint

Purpose: Close SEARCH-01, SEARCH-07, and UI-07 gaps by correcting frontend API calls from broken /products endpoint to working /search endpoint

Output: Working product search functionality with tenant-scoped results
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-critical-frontend-fixes/14-RESEARCH.md
@.planning/phases/14-critical-frontend-fixes/14-VALIDATION.md

# Backend Routes (Already Working)
From routes/api.php:
```php
Route::get('/tenants/{tenantId}/search', [ProductSearchController::class, 'search']);
```

# Current Broken Implementation
From public/js/dashboard.js:467:
```javascript
const response = await fetch(`/api/v1/tenants/${this.tenantId}/products?query=${encodeURIComponent(this.searchQuery)}&page=${this.currentPage}`, {
```

# Fix Required
Change endpoint from `/products` to `/search` (1-line change per file)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix product search endpoint in main dashboard</name>
  <files>public/js/dashboard.js, tests/Feature/ProductSearchUIIntegrationTest.php</files>
  <behavior>
    GREEN Phase - Implement actual test assertions:
    - test_dashboard_search_calls_correct_search_endpoint: Verify fetch() calls /search not /products
    - test_dashboard_search_includes_tenant_id_in_request: Verify tenantId in URL
    - test_dashboard_search_handles_api_errors: Verify error handling works
    - test_dashboard_search_updates_ui_with_results: Verify UI updates with results

    Replace assertTrue(true) with real assertions that verify endpoint correction
  </behavior>
  <action>
    File: public/js/dashboard.js, line 467
    Change: `/api/v1/tenants/${this.tenantId}/products`
    To: `/api/v1/tenants/${this.tenantId}/search`

    This is a simple string replacement in the fetch() URL.
    No other changes needed - query parameters remain the same.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/ProductSearchUIIntegrationTest.php</automated>
  </verify>
  <done>Dashboard product search calls correct /search endpoint, tests passing with real assertions</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix product search endpoint in reusable component</name>
  <files>resources/js/components/product-search.js, tests/Feature/ProductSearchEndpointTest.php</files>
  <behavior>
    GREEN Phase - Implement actual test assertions:
    - test_product_search_endpoint_returns_200_for_authenticated_user: Real API call test
    - test_product_search_endpoint_requires_authentication: Verify 401 without auth
    - test_product_search_results_scoped_to_tenant: Verify tenant isolation
    - test_product_search_accepts_query_and_page_parameters: Verify parameter handling

    Replace assertTrue(true) with real API endpoint tests
  </behavior>
  <action>
    File: resources/js/components/product-search.js, line 33
    Change: `/api/v1/tenants/${this.tenantId}/products`
    To: `/api/v1/tenants/${this.tenantId}/search`

    This is a simple string replacement in the fetch() URL.
    No other changes needed - query parameters remain the same.
  </action>
  <verify>
    <automated>./vendor/bin/pest tests/Feature/ProductSearchEndpointTest.php</automated>
  </verify>
  <done>Product search component calls correct /search endpoint, tests passing with real assertions</done>
</task>

</tasks>

<verification>
Product search integration verified:
- Frontend calls GET /api/v1/tenants/{tenantId}/search (not /products)
- Backend route exists and returns 200 for authenticated users
- Search results scoped to selected tenant (SEARCH-07 verified)
- UI updates correctly with search results (UI-07 verified)
- All automated tests passing
</verification>

<success_criteria>
- [ ] public/js/dashboard.js line 467 changed from /products to /search
- [ ] resources/js/components/product-search.js line 33 changed from /products to /search
- [ ] ProductSearchEndpointTest tests passing with real assertions
- [ ] ProductSearchUIIntegrationTest tests passing with real assertions
- [ ] Manual test: Load dashboard, search for products, results appear
- [ ] SEARCH-01 requirement satisfied
- [ ] SEARCH-07 requirement satisfied
- [ ] UI-07 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/14-critical-frontend-fixes/14-01-SUMMARY.md`
</output>
