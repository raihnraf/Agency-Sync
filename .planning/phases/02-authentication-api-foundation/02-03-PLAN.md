---
phase: 02-authentication-api-foundation
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - tests/Feature/Api/ApiVersioningTest.php
  - tests/Feature/Api/HttpStatusCodesTest.php
  - routes/api.php
  - bootstrap/app.php
  - app/Http/Controllers/Api/V1/AuthController.php
autonomous: false
requirements:
  - API-02
  - API-04

must_haves:
  truths:
    - "API endpoints are versioned with /api/v1/ prefix"
    - "API uses appropriate HTTP status codes (200, 201, 204, 400, 401, 404, 422, 429, 500)"
    - "API follows RESTful design principles (proper HTTP verbs, noun-based routes)"
    - "Version prefix allows future API versions without breaking existing clients"
  artifacts:
    - path: "tests/Feature/Api/ApiVersioningTest.php"
      provides: "Test coverage for API versioning"
      min_lines: 30
    - path: "tests/Feature/Api/HttpStatusCodesTest.php"
      provides: "Test coverage for proper HTTP status codes"
      min_lines: 50
    - path: "routes/api.php"
      provides: "Versioned API route definitions"
      contains: "Route::prefix('v1')"
  key_links:
    - from: "routes/api.php"
      to: "all API endpoints"
      via: "Route::prefix('v1') wrapper"
      pattern: "Route::prefix\\('v1'\\)"
    - from: "app/Http/Controllers/Api/V1/*Controller.php"
      to: "HTTP status codes"
      via: "response helper methods"
      pattern: "\\$this->success\\(.*\\, 200\\)|\\$this->created\\(.*\\, 201\\)|\\$this->error\\(.*\\, 4[0-9][0-9]\\)"
---

<objective>
Implement API versioning structure and ensure all endpoints use appropriate HTTP status codes following RESTful design principles.

Purpose: Provide versioned API endpoints (/api/v1/) that allow future API evolution without breaking existing clients, and use semantically correct HTTP status codes for all response scenarios.

Output: Complete API versioning structure, proper HTTP status codes across all endpoints, test coverage, and RESTful route organization.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication-api-foundation/02-CONTEXT.md
@.planning/phases/02-authentication-api-foundation/02-RESEARCH.md
@.planning/REQUIREMENTS.md

# Existing Implementation from Plans 01-02

From Plan 02-01:
- routes/api.php created with Route::prefix('v1') wrapper
- AuthController with register, login, logout endpoints
- Form Request validation with custom error formatting

From Plan 02-02:
- Base API controller with success(), error(), created(), noContent() helpers
- AuthController refactored to use response helpers

# Decisions from CONTEXT.md

**API Versioning (Locked Decision):**
- "URL-based versioning — /api/v1/ prefix for all endpoints"
- "Version 1 only — No backward compatibility needed for v1"

**HTTP Status Codes (Locked Decision):**
- "Full RESTful semantics — Use appropriate HTTP status codes"
  - 200 OK — Successful GET/PATCH
  - 201 Created — Successful POST
  - 204 No Content — Successful DELETE
  - 400 Bad Request — Malformed request
  - 401 Unauthorized — Missing or invalid token
  - 403 Forbidden — Valid token but insufficient permissions
  - 404 Not Found — Resource doesn't exist
  - 422 Unprocessable Entity — Validation failed
  - 429 Too Many Requests — Rate limit exceeded
  - 500 Internal Server Error — Server error

# RESTful Design Principles

From RESEARCH.md:
- Proper HTTP verbs (GET, POST, PUT/PATCH, DELETE)
- Noun-based routes (/users, /products, not /getUsers)
- Resource controllers for CRUD operations
- Route grouping for middleware and prefixes
</context>

<tasks>

<task type="auto">
  <name>Wave 0: Create API versioning and status code test stubs</name>
  <files>
    tests/Feature/Api/ApiVersioningTest.php
    tests/Feature/Api/HttpStatusCodesTest.php
  </files>
  <action>
Create test stub files for API versioning and HTTP status code verification.

Create tests/Feature/Api/ApiVersioningTest.php:
- Namespace: Tests\Feature\Api
- Extends TestCase
- Use RefreshDatabase trait
- Placeholder test: test_api_endpoints_use_v1_prefix()
- Placeholder test: test_unversioned_endpoint_returns_404()

Create tests/Feature/Api/HttpStatusCodesTest.php:
- Namespace: Tests\Feature\Api
- Extends TestCase
- Use RefreshDatabase trait
- Placeholder test: test_successful_registration_returns_201()
- Placeholder test: test_successful_login_returns_200()
- Placeholder test: test_successful_logout_returns_204()
- Placeholder test: test_validation_error_returns_422()
- Placeholder test: test_authentication_error_returns_401()
- Placeholder test: test_not_found_endpoint_returns_404()

All stubs should have empty test methods or $this->assertTrue(true) to allow PHPUnit to run.
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=ApiVersioningTest && ./vendor/bin/phpunit --testsuite=Feature --filter=HttpStatusCodesTest</verify>
  <done>Two test stub files created with placeholder tests that pass</done>
</task>

<task type="auto">
  <name>Task 1: Verify API versioning structure in routes</name>
  <files>
    routes/api.php
  </files>
  <action>
Verify that routes/api.php has proper versioning structure and add any missing components.

Check existing routes/api.php:
- Should have Route::prefix('v1') wrapping all routes
- Should have clear separation between public and protected routes
- Should follow RESTful naming (register, login, logout, not postRegister, etc.)

Add if missing:
- API route group with proper middleware structure
- Comment header indicating API v1
- Organize routes by resource (auth endpoints grouped)

Example structure (verify matches):
```php
<?php

use Illuminate\Support\Facades\Route;
use App\Http\Controllers\Api\V1\AuthController;

/*
|--------------------------------------------------------------------------
| API v1 Routes
|--------------------------------------------------------------------------
*/

Route::prefix('v1')->group(function () {
    // Public routes
    Route::post('/register', [AuthController::class, 'register']);
    Route::post('/login', [AuthController::class, 'login']);

    // Protected routes (require authentication)
    Route::middleware('auth:sanctum')->group(function () {
        Route::post('/logout', [AuthController::class, 'logout']);
        Route::get('/me', [AuthController::class, 'me']);
    });
});
```

Per CONTEXT.md decision: "URL-based versioning — /api/v1/ prefix for all endpoints".
  </action>
  <verify>grep -n "Route::prefix('v1')" routes/api.php && head -20 routes/api.php | grep -q "API v1"</verify>
  <done>API routes properly versioned with /api/v1/ prefix and organized structure</done>
</task>

<task type="auto">
  <name>Task 2: Verify HTTP status codes in AuthController</name>
  <files>
    app/Http/Controllers/Api/V1/AuthController.php
  </files>
  <action>
Verify that AuthController uses appropriate HTTP status codes for all response scenarios.

Check register():
- Success: returns $this->created() (status 201)
- Validation: FormRequest returns 422 automatically
- Already handled by base controller response helpers

Check login():
- Success: returns $this->success() with status 200 (default)
- Invalid credentials: returns $this->error() with status 401
- Validation: FormRequest returns 422 automatically

Check logout():
- Success: returns $this->noContent() (status 204)
- Unauthenticated: auth:sanctum middleware returns 401 automatically

Check me() method (if exists):
- Success: returns $this->success() with status 200
- Unauthenticated: auth:sanctum middleware returns 401 automatically

Add me() method if missing:
```php
public function me(Request $request)
{
    return $this->success([
        'user' => new UserResource($request->user()),
    ]);
}
```

Verify all status codes match CONTEXT.md locked decision:
- 200 OK: Successful GET/PATCH ✓
- 201 Created: Successful POST (register) ✓
- 204 No Content: Successful DELETE/LOGOUT ✓
- 401 Unauthorized: Missing/invalid token ✓
- 422 Unprocessable Entity: Validation failed ✓

Per CONTEXT.md decision: "Full RESTful semantics — Use appropriate HTTP status codes".
  </action>
  <verify>grep -n "\\$this->created" app/Http/Controllers/Api/V1/AuthController.php && grep -n "\\$this->success" app/Http/Controllers/Api/V1/AuthController.php && grep -n "\\$this->noContent" app/Http/Controllers/Api/V1/AuthController.php && grep -n "401" app/Http/Controllers/Api/V1/AuthController.php</verify>
  <done>All AuthController endpoints use appropriate HTTP status codes</done>
</task>

<task type="auto">
  <name>Task 3: Configure Laravel API route handling</name>
  <files>
    bootstrap/app.php
  </files>
  <action>
Verify that bootstrap/app.php properly configures API routing and Sanctum middleware.

Check bootstrap/app.php:
- Should have ->withRouting() configured for api.php
- Should have Sanctum middleware configured
- Should have proper middleware aliases

Verify API routes are enabled:
```php
->withRouting(
    web: __DIR__.'/../routes/web.php',
    api: __DIR__.'/../routes/api.php',  // Should exist
    commands: __DIR__.'/../routes/console.php',
    health: '/up',
)
```

Verify Sanctum middleware is configured:
```php
->withMiddleware(function (Middleware $middleware) {
    $middleware->api(prepend: [
        \Laravel\Sanctum\Http\Middleware\EnsureFrontendRequestsAreStateful::class,
    ]);
})
```

If API routes are not explicitly configured, add the api: parameter to withRouting().
Per CONTEXT.md decision: "API endpoints are versioned (/api/v1/)".
  </action>
  <verify>grep -n "api:" bootstrap/app.php && grep -n "Sanctum" bootstrap/app.php</verify>
  <done>bootstrap/app.php configured for API routing and Sanctum middleware</done>
</task>

<task type="auto">
  <name>Task 4: Implement API versioning tests</name>
  <files>
    tests/Feature/Api/ApiVersioningTest.php
  </files>
  <action>
Replace placeholder tests with full implementations verifying API versioning structure.

ApiVersioningTest.php:

test_api_endpoints_use_v1_prefix():
- POST /api/v1/register → assertStatus(422 or 201)
- Proves /api/v1/ prefix works

test_unversioned_endpoint_returns_404():
- POST /api/register (without v1)
- assertStatus(404)
- Proves version prefix is required

test_api_v1_routes_are_isolated():
- Create route /api/v1/test (temporary test route)
- POST /api/test returns 404
- POST /api/v1/test returns 200 (if implemented)
- Remove test route after verification

test_future_api_versions_can_coexist():
- Add comment describing how v2 would be added:
  "Future: Add Route::prefix('v2') group for API v2"
- Verify current structure supports multiple versions

Per CONTEXT.md decision: "Version 1 only — No backward compatibility needed for v1".
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=ApiVersioningTest</verify>
  <done>All API versioning tests pass (4 tests)</done>
</task>

<task type="auto">
  <name>Task 5: Implement HTTP status code tests</name>
  <files>
    tests/Feature/Api/HttpStatusCodesTest.php
  </files>
  <action>
Replace placeholder tests with full implementations verifying proper HTTP status codes.

HttpStatusCodesTest.php:

test_successful_registration_returns_201():
- POST /api/v1/register with valid data
- assertStatus(201)
- Proves POST creation returns 201 Created

test_successful_login_returns_200():
- Create user, POST /api/v1/login
- assertStatus(200)
- Proves successful GET/PATCH returns 200 OK

test_successful_logout_returns_204():
- Login, POST /api/v1/logout with token
- assertStatus(204)
- assertContent('') (empty body)
- Proves successful DELETE returns 204 No Content

test_validation_error_returns_422():
- POST /api/v1/register with invalid data
- assertStatus(422)
- assertJsonValidationErrors()
- Proves validation failures return 422 Unprocessable Entity

test_authentication_error_returns_401():
- POST /api/v1/logout without token
- assertStatus(401)
- Proves missing auth returns 401 Unauthorized

test_invalid_credentials_return_401():
- POST /api/v1/login with wrong password
- assertStatus(401)
- assertJsonPath('errors.0.field', 'email')
- Proves auth failures return 401

test_not_found_endpoint_returns_404():
- POST /api/v1/nonexistent
- assertStatus(404)
- Proves unknown endpoints return 404 Not Found

Per CONTEXT.md decision: "Full RESTful semantics — Use appropriate HTTP status codes".
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=HttpStatusCodesTest</verify>
  <done>All HTTP status code tests pass (7 tests)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete API versioning structure with /api/v1/ prefix, proper HTTP status codes across all endpoints, RESTful route organization, and comprehensive test coverage</what-built>
  <how-to-verify>
1. Ensure Docker containers are running: make up
2. Test API versioning (with v1 prefix):
   curl -X POST http://localhost/api/v1/register \
     -H "Content-Type: application/json" \
     -d '{"name":"Test User","email":"test3@example.com","password":"password123","password_confirmation":"password123"}'
   Expected: 201 Created (v1 prefix works)
3. Test unversioned endpoint (should fail):
   curl -X POST http://localhost/api/register \
     -H "Content-Type: application/json" \
     -d '{"name":"Test User","email":"test4@example.com","password":"password123"}'
   Expected: 404 Not Found (version required)
4. Test HTTP status codes:
   - 201: Successful registration (see step 2)
   - 200: curl -X POST http://localhost/api/v1/login -H "Content-Type: application/json" -d '{"email":"test3@example.com","password":"password123"}'
   - 204: curl -X POST http://localhost/api/v1/logout -H "Authorization: Bearer YOUR_TOKEN"
   - 401: curl -X POST http://localhost/api/v1/logout (without token)
   - 422: curl -X POST http://localhost/api/v1/register -H "Content-Type: application/json" -d '{"name":"","email":"invalid"}'
   - 404: curl -X POST http://localhost/api/v1/nonexistent -H "Content-Type: application/json" -d '{}'
5. Run test suite: docker-compose exec app ./vendor/bin/phpunit --testsuite=Feature --filter=ApiVersioningTest && docker-compose exec app ./vendor/bin/phpunit --testsuite=Feature --filter=HttpStatusCodesTest
   Expected: All 11 tests pass (4 versioning tests + 7 status code tests)
  </how-to-verify>
  <resume-signal>Type "approved" if API versioning and status codes are correct, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Overall plan verification:
1. All API endpoints use /api/v1/ prefix
2. Unversioned endpoints return 404
3. HTTP status codes match RESTful semantics:
   - 200 for successful GET/PATCH
   - 201 for successful POST (creation)
   - 204 for successful DELETE/logout
   - 401 for missing/invalid authentication
   - 422 for validation failures
   - 404 for unknown endpoints
4. All tests pass (11 tests total)
</verification>

<success_criteria>
- [ ] API routes properly versioned with Route::prefix('v1')
- [ ] Unversioned endpoints return 404
- [ ] Registration returns 201 Created on success
- [ ] Login returns 200 OK on success
- [ ] Logout returns 204 No Content on success
- [ ] Validation failures return 422 Unprocessable Entity
- [ ] Authentication failures return 401 Unauthorized
- [ ] Unknown endpoints return 404 Not Found
- [ ] All API versioning tests pass (4 tests)
- [ ] All HTTP status code tests pass (7 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-api-foundation/02-03-SUMMARY.md`
</output>
