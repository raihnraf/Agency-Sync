---
phase: 02-authentication-api-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/Feature/Api/JsonResponseTest.php
  - tests/Feature/Api/ValidationTest.php
  - app/Http/Resources/ApiResponseResource.php
  - app/Http/Controllers/Api/V1/Controller.php
  - app/Providers/AppServiceProvider.php
autonomous: false
requirements:
  - API-01
  - API-03
  - API-06
  - API-07

must_haves:
  truths:
    - "API returns consistent JSON response structure with data, meta, and errors fields"
    - "API Resources transform models to standardized JSON format"
    - "Request validation fails return field-level errors in consistent format"
    - "API responses follow RESTful design principles (proper HTTP verbs, noun-based routes)"
  artifacts:
    - path: "tests/Feature/Api/JsonResponseTest.php"
      provides: "Test coverage for consistent JSON response structure"
      min_lines: 35
    - path: "tests/Feature/Api/ValidationTest.php"
      provides: "Test coverage for validation error formatting"
      min_lines: 40
    - path: "app/Http/Resources/ApiResponseResource.php"
      provides: "Base API response wrapper for consistent structure"
      min_lines: 25
    - path: "app/Http/Controllers/Api/V1/Controller.php"
      provides: "Base controller with common response helper methods"
      min_lines: 40
  key_links:
    - from: "app/Http/Controllers/Api/V1/Controller.php"
      to: "app/Http/Resources/ApiResponseResource.php"
      via: "success() and error() methods"
      pattern: "ApiResponseResource::make|new ApiResponseResource"
    - from: "app/Http/Requests/*Request.php"
      to: "validation errors"
      via: "failedValidation() override"
      pattern: "HttpResponseException.*response\\(\\)->json"
    - from: "tests/Feature/Api/*Test.php"
      to: "API response structure"
      via: "assertJson() and assertJsonStructure() assertions"
      pattern: "\\$response->assertJson\\("
---

<objective>
Establish consistent JSON response structure and validation error formatting for all API endpoints using Laravel API Resources and Form Request validation.

Purpose: Ensure API clients receive predictable, consistent responses across all endpoints, enabling reliable client-side error handling and data display.

Output: Base API controller with response helpers, API response wrapper resource, validation error formatting, and test coverage.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication-api-foundation/02-CONTEXT.md
@.planning/phases/02-authentication-api-foundation/02-RESEARCH.md
@.planning/REQUIREMENTS.md

# Decisions from CONTEXT.md

**API Response Format (Locked Decision):**
- Wrapped responses: `{data: {...}, meta: {...}, errors: [...]}`
- Success responses: `{data: {...}, meta: {page: 1, per_page: 20, total: 100}}`
- Error responses: `{errors: [{field: "email", message: "Invalid email"}]}`
- Field-based validation errors: Array of errors with field and message properties
- Supports multiple errors: Clients can display all validation issues at once

**Claude's Discretion:**
- Response metadata fields beyond pagination (we'll keep it simple: just pagination for now)

# Patterns from RESEARCH.md

**API Resources Pattern:**
- Disable wrapping: `public static $wrap = null`
- Use `collection()` and `additional()` for metadata
- Transform models consistently with toArray()

**Validation Error Pattern:**
- Override `failedValidation()` in Form Request classes
- Flatten Laravel's default `{field: [messages]}` to `{errors: [{field, message}]}`
- Return 422 status with HttpResponseException

**RESTful Design:**
- Proper HTTP verbs (GET, POST, PUT/PATCH, DELETE)
- Noun-based routes (/users, /products, not /getUsers)
- Resource controllers for CRUD operations
</context>

<tasks>

<task type="auto">
  <name>Wave 0: Create API response and validation test stubs</name>
  <files>
    tests/Feature/Api/JsonResponseTest.php
    tests/Feature/Api/ValidationTest.php
  </files>
  <action>
Create test stub files for API response structure and validation error formatting.

Create tests/Feature/Api/JsonResponseTest.php:
- Namespace: Tests\Feature\Api
- Extends TestCase
- Use RefreshDatabase trait
- Placeholder test: test_api_returns_consistent_json_structure()
- Placeholder test: test_success_response_includes_data_field()
- Placeholder test: test_error_response_includes_errors_array()

Create tests/Feature/Api/ValidationTest.php:
- Namespace: Tests\Feature\Api
- Extends TestCase
- Use RefreshDatabase trait
- Placeholder test: test_validation_errors_return_field_level_errors()
- Placeholder test: test_multiple_validation_errors_supported()
- Placeholder test: test_validation_returns_422_status()

All stubs should have empty test methods or $this->assertTrue(true) to allow PHPUnit to run.
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=JsonResponse && ./vendor/bin/phpunit --testsuite=Feature --filter=Validation</verify>
  <done>Two test stub files created with placeholder tests that pass</done>
</task>

<task type="auto">
  <name>Task 1: Create base API controller with response helpers</name>
  <files>
    app/Http/Controllers/Api/V1/Controller.php
  </files>
  <action>
Create base controller class with helper methods for consistent API responses.

Create app/Http/Controllers/Api/V1/Controller.php:
- Namespace: App\Http\Controllers\Api\V1
- Extends Controller

Add methods:

success($data, $meta = [], $status = 200):
- Builds response array: ['data' => $data, 'meta' => $meta]
- Returns response()->json($array, $status)

error($errors, $status = 400):
- If $errors is string: wrap in array
- If $errors is array but not field-level: wrap as [{message: $errors}]
- If $errors is already field-level: use as-is
- Returns response()->json(['errors' => $errors], $status)

created($data, $meta = []):
- Calls success($data, $meta, 201)

noContent():
- Returns response()->json(null, 204)

Per CONTEXT.md decision: "All responses use consistent structure: {data: {...}, meta: {...}, errors: [...]}".
  </action>
  <verify>grep -n "class Controller" app/Http/Controllers/Api/V1/Controller.php && grep -n "function success" app/Http/Controllers/Api/V1/Controller.php</verify>
  <done>Base API controller created with response helper methods</done>
</task>

<task type="auto">
  <name>Task 2: Update Form Requests to use consistent error formatting</name>
  <files>
    app/Http/Requests/RegisterRequest.php
    app/Http/Requests/LoginRequest.php
  </files>
  <action>
Ensure all Form Request classes override failedValidation() to return consistent error format.

Verify app/Http/Requests/RegisterRequest.php:
- Check that failedValidation() method exists
- Format errors as: {errors: collect($validator->errors())->map(...)->values()}
- Return HttpResponseException with response()->json(['errors' => $errors], 422)

Verify app/Http/Requests/LoginRequest.php:
- Same error formatting as RegisterRequest
- Same 422 status code

If either file is missing the override, add it using the pattern from RESEARCH.md:
```php
protected function failedValidation(Validator $validator)
{
    $errors = collect($validator->errors())
        ->map(function ($messages, $field) {
            return [
                'field' => $field,
                'message' => $messages[0],
            ];
        })
        ->values();

    throw new HttpResponseException(
        response()->json(['errors' => $errors], 422)
    );
}
```

Per CONTEXT.md decision: "Field-based validation errors — Array of errors with field and message properties".
  </action>
  <verify>grep -n "failedValidation" app/Http/Requests/RegisterRequest.php && grep -n "failedValidation" app/Http/Requests/LoginRequest.php</verify>
  <done>All Form Requests use consistent error formatting with field-level errors</done>
</task>

<task type="auto">
  <name>Task 3: Update AuthController to use base controller response helpers</name>
  <files>
    app/Http/Controllers/Api/V1/AuthController.php
  </files>
  <action>
Refactor AuthController to extend base Controller and use response helper methods.

Update app/Http/Controllers/Api/V1/AuthController.php:
- Change: extends Controller → extends base Controller (use App\Http\Controllers\Api\V1\Controller)
- Import: use App\Http\Controllers\Api\V1\Controller

Refactor register method:
- Return: $this->created([...data...], $meta)
- Replace: response()->json([...], 201) → $this->created([...])

Refactor login method:
- Success: $this->success([...data...], $meta)
- Error: $this->error([...errors...], 401)

Refactor logout method:
- Return: $this->noContent()
- Replace: response()->json(null, 204) → $this->noContent()

Ensure all responses maintain consistent structure:
- Success: {data: {...}, meta: {...}}
- Error: {errors: [{field, message}]}
  </action>
  <verify>grep -n "extends Controller" app/Http/Controllers/Api/V1/AuthController.php && grep -n "\\$this->success\|\\$this->error\|\\$this->created\|\\$this->noContent" app/Http/Controllers/Api/V1/AuthController.php</verify>
  <done>AuthController refactored to use base controller response helpers</done>
</task>

<task type="auto">
  <name>Task 4: Implement API response structure tests</name>
  <files>
    tests/Feature/Api/JsonResponseTest.php
  </files>
  <action>
Replace placeholder tests with full implementations verifying consistent JSON response structure.

JsonResponseTest.php:

test_api_returns_consistent_json_structure():
- POST /api/v1/register with valid data
- Assert response has exact structure: {data: {...}, meta: {...}}
- Assert data contains user and token
- Assert meta contains expires_at

test_success_response_includes_data_field():
- POST /api/v1/login
- assertJsonPath('data.user.email', $user->email)
- assertJsonPath('data.token') is not null
- assertJsonPath('meta.expires_at') is not null

test_error_response_includes_errors_array():
- POST /api/v1/login with invalid credentials
- assertJsonPath('errors') is array
- assertJsonPath('errors.0.field') exists
- assertJsonPath('errors.0.message') exists

test_no_content_response_has_no_body():
- Login to get token
- POST /api/v1/logout with token
- assertStatus(204)
- assertContent(empty)

Use assertJson() and assertJsonStructure() for structure validation. Follow patterns from RESEARCH.md.
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=JsonResponseTest</verify>
  <done>All API response structure tests pass (4 tests)</done>
</task>

<task type="auto">
  <name>Task 5: Implement validation error formatting tests</name>
  <files>
    tests/Feature/Api/ValidationTest.php
  </files>
  <action>
Replace placeholder tests with full implementations verifying validation error formatting.

ValidationTest.php:

test_validation_errors_return_field_level_errors():
- POST /api/v1/register with missing name and invalid email
- assertStatus(422)
- assertJsonStructure(['errors' => [['field', 'message']]])
- Assert at least 2 errors in array

test_multiple_validation_errors_supported():
- POST /api/v1/register with empty body
- assertStatus(422)
- assertJsonPath('errors') has multiple items
- Assert errors for name, email, password fields

test_validation_returns_422_status():
- POST /api/v1/register with invalid data
- assertStatus(422)
- assertJsonValidationErrors() helper for specific fields

test_login_validation_requires_email():
- POST /api/v1/login without email
- assertStatus(422)
- assertJsonPath('errors.0.field', 'email')

test_password_mismatch_returns_error():
- POST /api/v1/register with password != password_confirmation
- assertStatus(422)
- assertJsonPath('errors.*.field', 'password_confirmation')

Per CONTEXT.md decision: "Supports multiple errors — Clients can display all validation issues at once".
  </action>
  <verify>./vendor/bin/phpunit --testsuite=Feature --filter=ValidationTest</verify>
  <done>All validation error formatting tests pass (5 tests)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Consistent JSON response structure and validation error formatting system with base API controller, response helpers, and comprehensive test coverage</what-built>
  <how-to-verify>
1. Ensure Docker containers are running: make up
2. Test success response structure:
   curl -X POST http://localhost/api/v1/register \
     -H "Content-Type: application/json" \
     -d '{"name":"Test User","email":"test2@example.com","password":"password123","password_confirmation":"password123"}' | jq .
   Expected: {data: {user: {...}, token: "..."}, meta: {expires_at: "..."}}
3. Test validation error structure:
   curl -X POST http://localhost/api/v1/register \
     -H "Content-Type: application/json" \
     -d '{"name":"","email":"invalid","password":"short"}' | jq .
   Expected: {errors: [{field: "name", message: "..."}, {field: "email", message: "..."}, {field: "password", message: "..."}]}
4. Test authentication error structure:
   curl -X POST http://localhost/api/v1/login \
     -H "Content-Type: application/json" \
     -d '{"email":"test@example.com","password":"wrong"}' | jq .
   Expected: {errors: [{field: "email", message: "..."}]}
5. Run test suite: docker-compose exec app ./vendor/bin/phpunit --testsuite=Feature --filter=JsonResponseTest && docker-compose exec app ./vendor/bin/phpunit --testsuite=Feature --filter=ValidationTest
   Expected: All 9 tests pass (4 response tests + 5 validation tests)
  </how-to-verify>
  <resume-signal>Type "approved" if JSON responses are consistent and validation errors are properly formatted, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Overall plan verification:
1. All API endpoints return consistent {data, meta} or {errors} structure
2. Validation errors are field-level with message property
3. Multiple validation errors supported in single response
4. Status codes are appropriate (200, 201, 204, 401, 422)
5. All tests pass (9 tests total)
</verification>

<success_criteria>
- [ ] Base API controller created with success(), error(), created(), noContent() helpers
- [ ] AuthController refactored to use base controller helpers
- [ ] All Form Requests override failedValidation() for consistent error format
- [ ] Success responses have {data: {...}, meta: {...}} structure
- [ ] Error responses have {errors: [{field, message}]} structure
- [ ] Multiple validation errors returned in single response
- [ ] All response structure tests pass (4 tests)
- [ ] All validation error tests pass (5 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-api-foundation/02-02-SUMMARY.md`
</output>
