---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.docker
  - Makefile
  - .gitignore
autonomous: false
requirements:
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "Developer can start entire stack with single command (`make up`)"
    - "Developer can stop stack with single command (`make down`)"
    - "Developer can view logs from all services with `make logs`"
    - "Developer can shell into app container with `make shell`"
    - "Environment variables are properly configured for all services"
    - "Sensitive environment files are excluded from git"
  artifacts:
    - path: ".env.docker"
      provides: "Docker-specific environment variables template"
      min_lines: 20
      contains: ["MYSQL_ROOT_PASSWORD", "ELASTICSEARCH_PASSWORD", "NGINX_PORT"]
    - path: "Makefile"
      provides: "Developer command interface for Docker operations"
      min_lines: 30
      contains: ["up:", "down:", "logs:", "shell:"]
    - path: ".gitignore"
      provides: "Git ignore rules for Docker environment files"
      min_lines: 5
      contains: ["^.env$", "^\\.env\\.local$"]
  key_links:
    - from: "Makefile"
      to: "compose.yaml"
      via: "docker compose commands"
      pattern: "docker compose\\s+up"
    - from: "compose.yaml"
      to: ".env.docker"
      via: "environment variable substitution"
      pattern: "\\${\\w+:-"
---

<objective>
Create developer workflow tools and environment configuration for seamless Docker development experience.

Purpose: Enable any developer to clone the repository and run `make up` to start the entire application stack with properly configured environment variables and secure handling of sensitive data.

Output: Working Makefile interface with all essential commands, environment template file, and proper .gitignore configuration.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

@.env.example
</context>

<interfaces>
<!-- No external interfaces needed - creates new developer tooling files -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Docker environment variables template</name>
  <files>.env.docker</files>
  <action>
    Create .env.docker template with Docker-specific environment variables:

    1. **MySQL variables**:
       - DB_ROOT_PASSWORD=root (or secure password)
       - DB_DATABASE=agencysync
       - DB_USERNAME=agencysync
       - DB_PASSWORD=secret

    2. **Elasticsearch variables**:
       - ELASTICSEARCH_HOST=elasticsearch
       - ELASTICSEARCH_PORT=9200

    3. **Redis variables**:
       - REDIS_HOST=redis
       - REDIS_PORT=6379

    4. **Nginx variables**:
       - NGINX_PORT=80

    5. **Application variables** (merge with existing .env.example):
       - APP_ENV=local
       - APP_DEBUG=true
       - APP_KEY=<generate>
       - DB_CONNECTION=mysql
       - DB_HOST=mysql
       - DB_PORT=3306
       - BROADCAST_DRIVER=log
       - CACHE_DRIVER=redis
       - FILESYSTEM_DISK=local
       - QUEUE_CONNECTION=redis
       - SESSION_DRIVER=redis
       - SESSION_LIFETIME=120

    Add comment at top: "# Docker-specific environment variables. Copy this file to .env and adjust values as needed."

    DO NOT commit sensitive values (use placeholder secrets).
  </action>
  <verify>
    <automated>grep -q "DB_HOST=mysql" .env.docker && grep -q "REDIS_HOST=redis" .env.docker && grep -q "ELASTICSEARCH_HOST=elasticsearch" .env.docker && echo "All service host variables present"</automated>
  </verify>
  <done>.env.docker exists with all Docker-specific variables, service hosts point to container names</done>
</task>

<task type="auto">
  <name>Task 2: Create Makefile developer interface</name>
  <files>Makefile</files>
  <action>
    Create Makefile with the following targets:

    1. **up**: Start all containers in detached mode
       - docker compose up -d
       - Echo message: "Containers started. Access app at http://localhost"

    2. **down**: Stop all containers
       - docker compose down
       - Echo message: "Containers stopped."

    3. **logs**: View logs from all containers (follow mode)
       - docker compose logs -f

    4. **shell**: Shell into app container
       - docker compose exec app bash

    5. **shell-nginx**: Shell into nginx container (for debugging)
       - docker compose exec nginx sh

    6. **shell-mysql**: Connect to MySQL CLI
       - docker compose exec mysql mysql -u$(DB_USERNAME) -p$(DB_PASSWORD) $(DB_DATABASE)

    7. **install**: Run composer install in app container
       - docker compose exec app composer install

    8. **migrate**: Run database migrations
       - docker compose exec app php artisan migrate

    9. **seed**: Seed database
       - docker compose exec app php artisan db:seed

    10. **test**: Run PHPUnit tests
        - docker compose exec app php artisan test

    11. **build**: First-time setup (build + install + setup)
        - docker compose build
        - docker compose run --rm app composer install
        - Copy .env.example to .env if not exists
        - Generate application key
        - Run migrations
        - Echo completion message

    Add .PHONY declaration for all targets (prevents Makefile conflicts with files of same name).
    Set .DEFAULT_GOAL to help (add help target that lists all available commands).

    Reference patterns from RESEARCH.md "Pattern 4: Makefile Developer Interface".
  </action>
  <verify>
    <automated>make -n up 2>&1 | grep -q "docker compose up" && echo "Makefile up target valid"</automated>
  </verify>
  <done>Makefile exists with up, down, logs, shell, install, migrate, test, build targets</done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore for Docker files</name>
  <files>.gitignore</files>
  <action>
    Append to existing .gitignore file (DO NOT remove existing entries):

    1. **Docker-generated files**:
       - .env (local environment, already in .gitignore)
       - .env.local (if created)
       - .env.docker.local (local overrides)

    2. **Docker volumes** (if any bind-mounted in project):
       - docker/volumes/* (if using bind mounts instead of named volumes)

    Ensure .env.example and .env.docker are NOT ignored (developers need these templates).

    Run: cat .gitignore to check current entries, then append Docker-specific ignores if not already present.
  </action>
  <verify>
    <automated>grep -q "^\.env$" .gitignore && echo ".env is ignored (correct)"</automated>
  </verify>
  <done>.gitignore updated with Docker-specific ignores, .env.docker and .env.example not ignored</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint: Developer Workflow Verification</name>
  <what-built>
    Complete developer workflow tooling with Makefile interface, environment configuration template, and proper .gitignore setup for secure development.
  </what-built>
  <how-to-verify>
    1. **Verify environment template**: Run `cat .env.docker`
       - Expected: All service host variables (DB_HOST=mysql, REDIS_HOST=redis, ELASTICSEARCH_HOST=elasticsearch)
       - Expected: NGINX_PORT=80 configured

    2. **Verify Makefile targets**: Run `make help` (or `cat Makefile`)
       - Expected: List of all available commands (up, down, logs, shell, etc.)

    3. **Test up command**: Run `make up`
       - Expected: "Containers started. Access app at http://localhost"
       - Expected: All containers start successfully

    4. **Test logs command**: Run `make logs`
       - Expected: Logs stream from all containers
       - Press Ctrl+C to exit

    5. **Test shell command**: Run `make shell`
       - Expected: Enters app container bash shell
       - Run `php artisan --version` to verify
       - Type `exit` to return to host

    6. **Test down command**: Run `make down`
       - Expected: "Containers stopped."
       - Expected: All containers stopped and removed

    7. **Verify .gitignore**: Run `cat .gitignore`
       - Expected: .env is ignored
       - Expected: .env.docker is NOT ignored (should be in repo)
  </how-to-verify>
  <resume-signal>Type "approved" if developer workflow is smooth, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval, verify the following:

1. **Environment completeness**: All required variables present in .env.docker
2. **Makefile targets**: All targets execute correct docker compose commands
3. **Git security**: .env is ignored, .env.docker is not ignored
4. **First-time setup**: New developer can clone and run `make up` successfully
5. **Command ergonomics**: All common operations available via make targets

Run all verification commands:
```bash
# Check environment
cat .env.docker
grep -E "DB_HOST|REDIS_HOST|ELASTICSEARCH_HOST" .env.docker

# Check Makefile
make -n up
make -n down
make -n logs

# Check gitignore
grep "^\.env$" .gitignore
grep "^\.env\.docker$" .gitignore && echo "ERROR: .env.docker should not be ignored" || echo "OK: .env.docker not ignored"
```
</verification>

<success_criteria>
1. .env.docker template exists with all required environment variables
2. Makefile provides up, down, logs, shell, shell-nginx, shell-mysql, install, migrate, seed, test, build targets
3. .gitignore excludes .env and .env.local but includes .env.docker and .env.example
4. make up starts all containers successfully
5. make down stops all containers cleanly
6. make logs shows aggregated logs from all services
7. make shell provides interactive bash access to app container
8. First-time setup works: clone repo → make up → application accessible
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Makefile targets created and their purposes
- Environment variables configured in .env.docker
- .gitignore rules added for Docker
- Developer onboarding steps documented
- Any deviations from standard patterns (and why)
</output>
