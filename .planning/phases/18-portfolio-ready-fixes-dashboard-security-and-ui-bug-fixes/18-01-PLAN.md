---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/js/dashboard.js
autonomous: true
requirements:
  - SYNC-06
  - UI-06
user_setup: []
must_haves:
  truths:
    - "Sync status displays in tenant detail view when visiting detail page"
    - "API route mismatch fixed - frontend calls correct endpoint"
    - "No 404 errors in browser console when loading sync status"
  artifacts:
    - path: "public/js/dashboard.js"
      provides: "Correct API route for sync status polling"
      min_lines: 1
      contains: "fetch(`/api/v1/sync-logs?tenant_id=${tenantId}&per_page=1`"
  key_links:
    - from: "public/js/dashboard.js"
      to: "/api/v1/sync-logs"
      via: "fetch with tenant_id query parameter"
      pattern: "fetch.*api/v1/sync-logs.*tenant_id"
---

<objective>
Fix sync status API route mismatch in frontend JavaScript to enable sync status display functionality

Purpose: Close critical integration gap where frontend calls wrong API endpoint, breaking sync status polling
Output: Working sync status display in tenant detail view
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md

@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/18-portfolio-ready-fixes-dashboard-security-and-ui-bug-fixes/18-RESEARCH.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Current Bug Details

From v1.0-MILESTONE-AUDIT.md:

**Frontend (dashboard.js:150):**
```javascript
const response = await fetch(`/api/v1/tenants/${tenantId}/sync-logs?per_page=1`, {
```

**Backend (routes/api.php:73):**
```php
Route::get('/sync-logs', [SyncLogController::class, 'index']);
// Expects: GET /api/v1/sync-logs?tenant_id={id}
```

**Impact:** Frontend gets 404 errors, sync status never displays in tenant detail view.

## Research Findings

From 18-RESEARCH.md:

- **Root Cause:** Phase 15 was planned but never executed (skipped in commit be660e0)
- **Fix:** Change dashboard.js line 150 to use query parameter format
- **Existing Pattern:** Line 576 already uses correct format: `fetch('/api/v1/sync-logs?${params}'`
- **Tenant Detail View:** resources/views/dashboard/tenants/show.blade.php already has sync status UI components
- **Tenant List View:** resources/views/dashboard/tenants/index.blade.php will be addressed in Plan 02

## Phase 14 Patterns

From Phase 14-03 SUMMARY:
- Single-character fixes are acceptable when they solve the core issue
- Frontend JavaScript fixes are simple one-line changes
- Verification via grep to confirm the fix is in place
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sync status API route mismatch in dashboard.js</name>
  <files>public/js/dashboard.js</files>
  <action>
    Fix the API route mismatch on line 150 of dashboard.js:

    **Current code (WRONG):**
    ```javascript
    const response = await fetch(`/api/v1/tenants/${tenantId}/sync-logs?per_page=1`, {
    ```

    **Fixed code (CORRECT):**
    ```javascript
    const response = await fetch(`/api/v1/sync-logs?tenant_id=${tenantId}&per_page=1`, {
    ```

    **Why this fix:**
    - Backend route is `GET /api/v1/sync-logs` with query parameter `tenant_id`
    - Frontend was calling non-existent route `/api/v1/tenants/{id}/sync-logs`
    - This pattern is already used correctly in dashboard.js line 576
    - Fixes SYNC-06 requirement: "Agency admin can view sync status for each client store"
    - Enables UI-06 requirement: "Agency admin can view last sync status for each client store"

    **Do NOT:**
    - Create new API endpoints (use existing /api/v1/sync-logs)
    - Modify backend routes (frontend needs to match existing API)
    - Change authentication (API already protected with Sanctum middleware)
  </action>
  <verify>
    <automated>grep -n 'fetch.*api/v1/sync-logs.*tenant_id' public/js/dashboard.js | head -5</automated>
  </verify>
  <done>
    - Line 150 of dashboard.js uses correct API route: `/api/v1/sync-logs?tenant_id=${tenantId}&per_page=1`
    - No changes to backend routes or authentication
    - Frontend matches existing API endpoint pattern from line 576
    - SYNC-06 requirement satisfied (sync status API call working)
  </done>
</task>

</tasks>

<verification>
1. Manual verification: Visit tenant detail view at http://localhost:8080/dashboard/tenants/{id}
2. Check browser console for no 404 errors
3. Verify "Last Sync" section displays sync status (time, status, product count)
4. If recent sync exists, status should show as "completed", "running", "pending", or "failed"
5. Sync status badge should be color-coded (green=completed, blue=running, red=failed, yellow=pending)
</verification>

<success_criteria>
- [ ] Frontend calls correct API endpoint: `/api/v1/sync-logs?tenant_id={id}`
- [ ] No 404 errors in browser console when loading tenant detail view
- [ ] Sync status displays in tenant detail view (time, status, product count)
- [ ] API route uses query parameter format (not path parameter)
- [ ] Backend route unchanged (still `/api/v1/sync-logs` with Sanctum middleware)
- [ ] SYNC-06 requirement satisfied (sync status API working)
</success_criteria>

<output>
After completion, create `.planning/phases/18-portfolio-ready-fixes-dashboard-security-and-ui-bug-fixes/18-01-SUMMARY.md`
</output>
