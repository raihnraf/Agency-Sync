---
phase: 08-hybrid-authentication
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified: [routes/web.php]
autonomous: true
requirements: [AUTH-WEB-02, AUTH-WEB-05]
user_setup: []
must_haves:
  truths:
    - Web routes use session-based authentication (auth middleware)
    - Dashboard routes protected by auth middleware
    - Unauthenticated users redirected to /login
    - API routes continue using Sanctum token authentication
    - Both authentication systems coexist without conflicts
  artifacts:
    - path: "routes/web.php"
      provides: "Web route definitions with session auth"
      contains: "Route::middleware(['auth'])"
    - path: "routes/api.php"
      provides: "API route definitions with token auth"
      contains: "Route::middleware(['auth:sanctum'])"
    - path: "app/Http/Middleware/Authenticate.php"
      provides: "Auth middleware for session-based authentication"
      min_lines: 20
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/Dashboard/*"
      via: "auth middleware protects dashboard routes"
      pattern: "Route::middleware\\(\\['auth'\\]\\)"
    - from: "routes/api.php"
      to: "app/Http/Controllers/Api/*"
      via: "auth:sanctum middleware protects API routes"
      pattern: "Route::middleware\\(\\['auth:sanctum'\\]\\)"
---

<objective>
Configure web routes to use session-based authentication while preserving Sanctum token authentication for API routes. Ensure dashboard routes are protected by auth middleware and redirect unauthenticated users to login page.

Purpose: Enable hybrid authentication system where web UI uses sessions (browser cookies) and API uses tokens (Bearer headers). Both systems coexist without conflicts, serving different client types (humans vs machines).

Output: Web routes use auth middleware, API routes use auth:sanctum middleware, unauthenticated dashboard access redirects to login.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-hybrid-authentication/08-CONTEXT.md
@.planning/phases/08-hybrid-authentication/08-RESEARCH.md

# From Wave 0: tests/Feature/Auth/SessionAuthTest.php and tests/Feature/Auth/ApiSanctumTest.php
# Tests verify web routes use sessions, API routes use Sanctum

# Existing codebase: routes/web.php already has dashboard routes with auth middleware (Phase 7)
# Existing codebase: routes/api.php has Sanctum middleware from Phase 2
# Research Pattern 2: Hybrid Authentication with separate middleware
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify web routes use auth middleware</name>
  <files>routes/web.php</files>
  <behavior>
    - Test: test_web_routes_use_auth_middleware verifies /dashboard/* routes have auth middleware
    - Test: test_login_logout_routes_exist verifies Breeze created auth routes
  </behavior>
  <action>
    Verify routes/web.php has correct middleware configuration:

    1. Read routes/web.php
    2. Confirm /dashboard/* routes wrapped with `Route::middleware(['auth'])` (should exist from Phase 7)
    3. Confirm Breeze added login/logout routes:
       - `Route::get('login', [AuthenticatedSessionController::class, 'create'])`
       - `Route::post('login', [AuthenticatedSessionController::class, 'store'])`
       - `Route::post('logout', [AuthenticatedSessionController::class, 'destroy'])`
    4. No code changes needed - Breeze installation in Plan 08-01 already configured this

    If dashboard routes missing auth middleware, add it:
    ```php
    Route::middleware(['auth'])->prefix('dashboard')->group(function () {
        // existing dashboard routes
    });
    ```
  </action>
  <verify>
    <automated>php artisan route:list --path=dashboard | grep -q "auth" && echo "Dashboard routes protected"</automated>
  </verify>
  <done>All /dashboard/* routes have auth middleware, login/logout routes registered</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify API routes use Sanctum middleware</name>
  <files>routes/api.php</files>
  <behavior>
    - Test: test_api_routes_use_sanctum_middleware verifies /api/* routes have auth:sanctum
    - Test: test_api_token_authentication_still_works verifies Sanctum tokens grant access
  </behavior>
  <action>
    Verify routes/api.php still uses Sanctum middleware (no changes from Phase 2):

    1. Read routes/api.php
    2. Confirm routes wrapped with `Route::middleware(['auth:sanctum'])`
    3. Confirm `auth:sanctum` middleware chain includes token expiration from Phase 2
    4. No code changes needed - Breeze does not modify API routes

    Breeze only adds routes to routes/web.php, not routes/api.php. API authentication remains unchanged.

    Verify middleware configuration in bootstrap/app.php (from Phase 2):
    ```php
    ->withMiddleware(function (Middleware $middleware) {
        $middleware->api(prepend: [
            \Laravel\Sanctum\Http\Middleware\EnsureFrontendRequestsAreStateless::class,
        ]);
    })
    ```
  </action>
  <verify>
    <automated>php artisan route:list --path=api | grep -q "auth:sanctum" && echo "API routes use Sanctum"</automated>
  </verify>
  <done>All /api/* routes have auth:sanctum middleware, no changes from Phase 2</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test unauthenticated redirect behavior</name>
  <files>routes/web.php, app/Http/Middleware/Authenticate.php</files>
  <behavior>
    - Test: test_unauthenticated_dashboard_redirects_to_login verifies guest users redirected
    - Test: test_unauthenticated_tenant_routes_redirect_to_login verifies all /dashboard/* routes redirect
  </behavior>
  <action>
    Verify unauthenticated users are redirected to login page:

    1. Test by visiting /dashboard/tenants without authentication
    2. Expected behavior: Redirect to /login with intended URL in session
    3. Laravel's default Authenticate middleware (app/Http/Middleware/Authenticate.php) handles this
    4. No code changes needed - Laravel's auth middleware redirects unauthenticated users to /login by default

    The redirect URL is stored in session, so after login user is redirected back to intended page.

    Verify redirect behavior:
    ```bash
    # Visit dashboard without authentication (should redirect to /login)
    curl -I http://localhost/dashboard/tenants
    # Expected: HTTP/1.1 302 Found, Location: http://localhost/login
    ```
  </action>
  <verify>
    <automated>php artisan test --filter=LoginRedirectTest</automated>
  </verify>
  <done>Unauthenticated /dashboard/* requests redirect to /login, session stores intended URL</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Verify session and token auth coexistence</name>
  <files>routes/web.php, routes/api.php, app/Models/User.php</files>
  <behavior>
    - Test: test_api_and_web_auth_coexist verifies both systems work independently
    - Test: test_api_routes_do_not_use_sessions verifies API rejects session cookies
  </behavior>
  <action>
    Verify both authentication systems work without conflicts:

    1. User model has both HasApiTokens trait (Sanctum) and session auth methods (Breeze)
    2. Web routes use `auth` middleware (default guard: web, uses sessions)
    3. API routes use `auth:sanctum` middleware (explicit guard: sanctum, uses tokens)
    4. No code changes needed - Laravel's guard system isolates the two auth systems

    Test coexistence:
    - Log in via browser (session auth) → access /dashboard routes
    - Create API token via `php artisan sanctuary:token` → access /api routes with Bearer token
    - Both work independently without conflicts

    The guard system prevents session-based auth from affecting API routes and vice versa.
  </action>
  <verify>
    <automated>php artisan test --filter=ApiSanctumTest</automated>
  </verify>
  <done>Session auth works for web routes, token auth works for API routes, no conflicts</done>
</task>

</tasks>

<verification>
Run authentication tests to verify hybrid auth system:

```bash
# Test session-based authentication for web routes
php artisan test --filter=SessionAuthTest

# Test Sanctum token authentication for API routes
php artisan test --filter=ApiSanctumTest

# Test redirect behavior for unauthenticated users
php artisan test --filter=LoginRedirectTest

# Verify route middleware configuration
php artisan route:list --path=dashboard
php artisan route:list --path=api
```

Expected result: Web routes use auth middleware, API routes use auth:sanctum, unauthenticated dashboard access redirects to login.
</verification>

<success_criteria>
- [ ] Web routes (routes/web.php) use auth middleware for session-based authentication
- [ ] API routes (routes/api.php) use auth:sanctum middleware for token authentication
- [ ] Unauthenticated /dashboard/* requests redirect to /login
- [ ] Session-based authentication works for web UI login
- [ ] Token-based authentication continues working for API endpoints
- [ ] No conflicts between session and token authentication systems
- [ ] User model supports both auth methods (HasApiTokens + session methods)
- [ ] Tests pass: SessionAuthTest, ApiSanctumTest, LoginRedirectTest
</success_criteria>

<output>
After completion, create `.planning/phases/08-hybrid-authentication/08-02-SUMMARY.md`
</output>
