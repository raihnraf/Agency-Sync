---
phase: 08-hybrid-authentication
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: [AUTH-WEB-01, AUTH-WEB-02, AUTH-WEB-03, AUTH-WEB-04, AUTH-WEB-05]
user_setup: []
must_haves:
  truths:
    - All test stub files exist with placeholder assertions
    - Test file structure follows Phase 2-7 PHPUnit patterns
    - Nyquist compliance achieved (tests written before implementation)
    - PHPUnit can discover and run all new test files
  artifacts:
    - path: "tests/Feature/Auth/BreezeInstallTest.php"
      provides: "Breeze installation verification"
      min_lines: 15
    - path: "tests/Feature/Auth/LoginTest.php"
      provides: "Login functionality testing"
      min_lines: 20
    - path: "tests/Feature/Auth/LoginRedirectTest.php"
      provides: "Login redirect behavior testing"
      min_lines: 15
    - path: "tests/Feature/Auth/SessionAuthTest.php"
      provides: "Session authentication testing"
      min_lines: 20
    - path: "tests/Feature/Auth/ApiSanctumTest.php"
      provides: "Sanctum API auth verification"
      min_lines: 15
    - path: "tests/Unit/Console/CustomAdminCommandTest.php"
      provides: "Artisan command unit testing"
      min_lines: 20
    - path: "tests/Feature/Auth/BladeCustomizationTest.php"
      provides: "Blade customization verification"
      min_lines: 15
  key_links:
    - from: "tests/Feature/Auth/*"
      to: "app/Http/Controllers/Auth/*"
      via: "Feature tests verify auth controller behavior"
      pattern: "test_.*login|logout|session"
    - from: "tests/Unit/Console/CustomAdminCommandTest.php"
      to: "app/Console/Commands/CreateAdminUser.php"
      via: "Unit tests verify command validation and user creation"
      pattern: "test_.*admin.*command"
---

<objective>
Create test stub foundation for Phase 8 hybrid authentication using TDD methodology. All test files are created with placeholder assertions before implementation begins, ensuring Nyquist compliance and establishing the test infrastructure for session-based authentication while preserving Sanctum token authentication.

Purpose: Enable test-driven development for Laravel Breeze installation, session authentication, API/Sanctum coexistence, admin user creation command, and Blade customization. Test stubs provide structural framework for RED→GREEN→REFACTOR workflow.

Output: Seven test files with placeholder assertions following existing Phase 2-7 PHPUnit patterns, establishing complete test coverage for all hybrid authentication requirements.
</objective>

<execution_context>
@/home/raihan/.claude/get-shit-done/workflows/execute-plan.md
@/home/raihan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/08-hybrid-authentication/08-CONTEXT.md
@.planning/phases/08-hybrid-authentication/08-RESEARCH.md
@.planning/phases/08-hybrid-authentication/08-VALIDATION.md

# Existing test patterns from Phase 2-4
# tests/Feature/Auth/* - Feature test structure with PHPUnit class-based syntax
# tests/Unit/Console/* - Unit test structure for command testing
# Placeholder assertions: $this->assertTrue(true) for Nyquist compliance
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Breeze installation verification test</name>
  <files>tests/Feature/Auth/BreezeInstallTest.php</files>
  <action>
    Create `tests/Feature/Auth/BreezeInstallTest.php` with placeholder test methods:

    1. `test_breeze_creates_auth_controllers()` - Verify Breeze installed auth controllers exist
    2. `test_auth_routes_exist()` - Verify login/logout routes are registered
    3. `test_session_driver_configured()` - Verify session driver is database
    4. `test_guest_middleware_exists()` - Verify RedirectIfAuthenticated middleware exists

    Use PHPUnit class-based syntax (extend TestCase). Use placeholder assertions `$this->assertTrue(true)` for all tests. Follow existing test file structure from Phase 2-4.

    Test file location: tests/Feature/Auth/BreezeInstallTest.php
    Purpose: Verify Laravel Breeze Blade stack installation creates required auth infrastructure
  </action>
  <verify>php artisan test --filter=BreezeInstallTest</verify>
  <done>Test file exists with 4 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 2: Create login functionality test</name>
  <files>tests/Feature/Auth/LoginTest.php</files>
  <action>
    Create `tests/Feature/Auth/LoginTest.php` with placeholder test methods:

    1. `test_login_page_accessible()` - Verify GET /login returns 200
    2. `test_user_can_login_with_valid_credentials()` - Verify valid email/password creates session
    3. `test_user_cannot_login_with_invalid_credentials()` - Verify invalid credentials show error
    4. `test_authenticated_user_redirected_from_login()` - Verify logged-in users redirected to dashboard
    5. `test_remember_me_creates_persistent_session()` - Verify remember me checkbox extends session

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Follow Phase 2-4 test patterns.

    Test file location: tests/Feature/Auth/LoginTest.php
    Purpose: Verify session-based login functionality works correctly
  </action>
  <verify>php artisan test --filter=LoginTest</verify>
  <done>Test file exists with 5 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 3: Create login redirect behavior test</name>
  <files>tests/Feature/Auth/LoginRedirectTest.php</files>
  <action>
    Create `tests/Feature/Auth/LoginRedirectTest.php` with placeholder test methods:

    1. `test_unauthenticated_dashboard_redirects_to_login()` - Verify guest users redirected to /login
    2. `test_unauthenticated_tenant_routes_redirect_to_login()` - Verify all /dashboard/* routes require auth
    3. `test_login_redirects_to_dashboard_after_success()` - Verify successful login redirects to /dashboard
    4. `test_logout_redirects_to_home()` - Verify logout redirects to / (welcome page)

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Follow Phase 7 test patterns for web routes.

    Test file location: tests/Feature/Auth/LoginRedirectTest.php
    Purpose: Verify auth middleware redirects work correctly per AUTH-WEB-05 requirement
  </action>
  <verify>php artisan test --filter=LoginRedirectTest</verify>
  <done>Test file exists with 4 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 4: Create session authentication test</name>
  <files>tests/Feature/Auth/SessionAuthTest.php</files>
  <action>
    Create `tests/Feature/Auth/SessionAuthTest.php` with placeholder test methods:

    1. `test_web_routes_use_session_middleware()` - Verify routes/web.php uses auth middleware
    2. `test_session_persists_across_requests()` - Verify authenticated session persists
    3. `test_session_expires_after_lifetime()` - Verify session expires after 2 hours
    4. `test_multiple_concurrent_sessions_allowed()` - Verify user can be logged in from multiple devices
    5. `test_logout_destroys_session()` - Verify logout invalidates session

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Reference Phase 2-4 auth test patterns.

    Test file location: tests/Feature/Auth/SessionAuthTest.php
    Purpose: Verify session-based authentication works correctly per AUTH-WEB-02 requirement
  </action>
  <verify>php artisan test --filter=SessionAuthTest</verify>
  <done>Test file exists with 5 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 5: Create Sanctum API verification test</name>
  <files>tests/Feature/Auth/ApiSanctumTest.php</files>
  <action>
    Create `tests/Feature/Auth/ApiSanctumTest.php` with placeholder test methods:

    1. `test_api_routes_use_sanctum_middleware()` - Verify routes/api.php still uses auth:sanctum
    2. `test_api_token_authentication_still_works()` - Verify existing Sanctum tokens grant access
    3. `test_api_routes_do_not_use_sessions()` - Verify API routes reject session cookies
    4. `test_sanctum_hasApiTokens_trait_unchanged()` - Verify User model still has HasApiTokens trait
    5. `test_api_and_web_auth_coexist()` - Verify both auth systems work independently

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Reference existing Phase 2 Sanctum tests as baseline.

    Test file location: tests/Feature/Auth/ApiSanctumTest.php
    Purpose: Verify API token authentication unchanged per AUTH-WEB-03 requirement (Breeze coexistence)
  </action>
  <verify>php artisan test --filter=ApiSanctumTest</verify>
  <done>Test file exists with 5 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 6: Create custom admin command test</name>
  <files>tests/Unit/Console/CustomAdminCommandTest.php</files>
  <action>
    Create `tests/Unit/Console/CustomAdminCommandTest.php` with placeholder test methods:

    1. `test_command_prompts_for_email()` - Verify command asks for email input
    2. `test_command_validates_email_format()` - Verify command rejects invalid email format
    3. `test_command_checks_email_uniqueness()` - Verify command rejects duplicate email
    4. `test_command_prompts_for_password()` - Verify command asks for password input
    5. `test_command_validates_password_length()` - Verify command enforces 8-char minimum
    6. `test_command_confirms_password()` - Verify command asks for password confirmation
    7. `test_command_creates_user_successfully()` - Verify command creates User record
    8. `test_command_outputs_login_url()` - Verify command displays login URL on success

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Follow existing Phase 2-4 unit test patterns.

    Test file location: tests/Unit/Console/CustomAdminCommandTest.php
    Purpose: Verify artisan command creates admin users per AUTH-WEB-04 requirement (CONTEXT.md decision)
  </action>
  <verify>php artisan test --filter=CustomAdminCommandTest</verify>
  <done>Test file exists with 8 placeholder test methods, PHPUnit discovers test class</done>
</task>

<task type="auto">
  <name>Task 7: Create Blade customization test</name>
  <files>tests/Feature/Auth/BladeCustomizationTest.php</files>
  <action>
    Create `tests/Feature/Auth/BladeCustomizationTest.php` with placeholder test methods:

    1. `test_login_page_has_agency_sync_logo()` - Verify AgencySync logo displayed
    2. `test_login_page_uses_indigo_color_scheme()` - Verify TailwindCSS indigo colors applied
    3. `test_login_page_has_custom_footer()` - Verify custom footer text present
    4. `test_registration_route_removed()` - Verify /register route returns 404
    5. `test_login_page_has_remember_me_checkbox()` - Verify remember me input exists

    Use PHPUnit class-based syntax. Use placeholder assertions `$this->assertTrue(true)`. Reference Phase 7 Dusk test patterns for Blade verification.

    Test file location: tests/Feature/Auth/BladeCustomizationTest.php
    Purpose: Verify login page customization per AUTH-WEB-04 requirement (CONTEXT.md branding decision)
  </action>
  <verify>php artisan test --filter=BladeCustomizationTest</verify>
  <done>Test file exists with 5 placeholder test methods, PHPUnit discovers test class</done>
</task>

</tasks>

<verification>
Run all Phase 8 tests to verify test infrastructure:

```bash
# Verify all new test files are discovered by PHPUnit
php artisan test --testsuite=Feature --filter=Auth
php artisan test --testsuite=Unit --filter=Console

# Verify specific test files
php artisan test --filter=BreezeInstallTest
php artisan test --filter=LoginTest
php artisan test --filter=LoginRedirectTest
php artisan test --filter=SessionAuthTest
php artisan test --filter=ApiSanctumTest
php artisan test --filter=CustomAdminCommandTest
php artisan test --filter=BladeCustomizationTest
```

Expected result: All tests pass (placeholder assertions return true), no test discovery errors.
</verification>

<success_criteria>
- [ ] All 7 test files created in appropriate directories
- [ ] Total of 36 placeholder test methods across all files
- [ ] PHPUnit discovers all test classes without errors
- [ ] All tests pass with placeholder assertions ($this->assertTrue(true))
- [ ] Test file structure follows Phase 2-7 PHPUnit patterns
- [ ] Nyquist compliance achieved: tests written before implementation
- [ ] Test infrastructure ready for RED→GREEN→REFACTOR TDD workflow
- [ ] Wave 0 complete: Implementation plans (08-01 through 08-06) can reference these tests
</success_criteria>

<output>
After completion, create `.planning/phases/08-hybrid-authentication/08-00-SUMMARY.md`
</output>
